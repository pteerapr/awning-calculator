# คู่มือแก้ไขราคาเครื่องคำนวณกันสาด (สำหรับเจ้าหน้าที่)

คู่มือนี้สำหรับ **เจ้าหน้าที่ธุรการ** ที่ต้องอัปเดตราคาในเครื่องคำนวณเมื่อมีราคาชุดใหม่จากโรงงาน
**ไม่ต้องเขียนโปรแกรมเป็นก็ทำได้** ขอแค่ทำตามขั้นตอนอย่างระมัดระวัง

> ระบบมีเครื่องคำนวณ **49 ตัว** และราคาเก็บอยู่ **หลายรูปแบบ** ไม่เหมือนกันทุกไฟล์
> **หัวข้อที่ 2 (ตารางค้นหา)** คือหัวใจของคู่มือนี้ — เปิดตรงนั้นก่อนเสมอ

---

## 0. เริ่มตรงนี้ — งานทั้งหมดมี 5 ขั้น

> พิมพ์หน้านี้แปะไว้ข้างจอได้เลย

| ขั้น | ทำอะไร | ดูหัวข้อ |
|---|---|---|
| **1** | ดับเบิลคลิก `backup.bat` — สำรองไฟล์ก่อนแก้ | § 3.3 |
| **2** | เปิด **ตารางค้นหา** หาว่าสินค้านั้นอยู่ไฟล์ไหน ค้นหาคำว่าอะไร | **§ 2** |
| **3** | แก้เฉพาะ **ตัวเลข** ในไฟล์นั้น แล้วกด `Ctrl + S` | § 5 |
| **4** | เพิ่มเลขเวอร์ชันใน `sw.js` (`v23` → `v24`) | **§ 7** |
| **5** | ดับเบิลคลิก `deploy.bat` → รอ `Deployment complete!` → เปิดเว็บจริงเช็ก | § 9 |

**ถ้าทำพัง** → ดู § 10 (ย้อนกลับได้ 3 ระดับ ไม่มีอะไรเสียหายถาวร)

---

## 1. กฎเหล็ก 7 ข้อ (อ่านก่อนแก้ทุกครั้ง)

1. **แก้ได้เฉพาะตัวเลข** เท่านั้น อย่าลบเครื่องหมาย `,` `[` `]` `{` `}` `"` `:` หรือ `//`
2. **ห้ามใส่ลูกน้ำในตัวเลข** — พิมพ์ `58070` ไม่ใช่ `58,070`
3. **แก้ทีละไฟล์ ทีละรุ่น** จะได้ตรวจง่ายถ้าผิด
4. **สำรองไฟล์ก่อนแก้เสมอ** — ดับเบิลคลิก `backup.bat` (ใช้เวลาไม่กี่วินาที)
5. **ทุกครั้งที่แก้ ต้องเพิ่มเลขเวอร์ชันใน `sw.js`** ไม่งั้นลูกค้าจะยังเห็นราคาเก่า (§ 7)
6. **เลขเวอร์ชัน `sw.js` มีแต่เพิ่มขึ้น ห้ามลด** — ถ้าเผลอลด ลูกค้าจะค้างอยู่กับราคาเก่าถาวร
7. **ทดสอบในเบราว์เซอร์ก่อน deploy เสมอ** อย่าเผยแพร่ทั้งที่ยังไม่ได้ลอง

---

## 2. ⭐ ตารางค้นหา — สินค้าตัวนี้อยู่ไฟล์ไหน?

**วิธีใช้:** หาชื่อสินค้าในคอลัมน์แรก → เปิดไฟล์ตามคอลัมน์ที่ 2 → กด `Ctrl + F` พิมพ์คำในคอลัมน์ที่ 3
→ อ่านวิธีแก้ของ "แบบ" นั้นในหัวข้อ 5

### 2.1 กันสาดพับแขน (Folding Arm Awning)

| สินค้า | เปิดไฟล์ | `Ctrl+F` หาคำว่า | แบบ | 1 แถวคืออะไร |
|---|---|---|---|---|
| Spettmann Star | `spettmann-star.html` | `PRICE_MANUAL_ACRYLIC` | ④ | มี 4 ตาราง: `_ACRYLIC` / `_SOLAR` × `MANUAL` (มือหมุน) / `MOTOR` (มอเตอร์) |
| Terra MA3 | `terra-ma3.html` | `PRICE_MANUAL_SUNBRELLA` | ④ | มี **8 ตาราง**: `_ACRYLIC` `_SOLAR` `_SUNBRELLA` `_NOVA` × `MANUAL`/`MOTOR` |
| Terra MA3 LED | `terra-ma3-LED.html` | `PRICE_MOTOR_ACRYLIC` | ④ | มีเฉพาะมอเตอร์ 2 ตาราง (`_ACRYLIC` `_SOLAR`) |
| Terra MA4 | `ma4-calculator.html` | `PRICE_TABLES` | ⑦ | ผ้า → มือหมุน/มอเตอร์ → ระยะยื่น |
| Chill D1 | `chill-d1.html` | `PRICE_MATRIX` | ⑥ | ช่วงกว้าง → ช่วงตก → ชนิดผ้า |

> ⚠️ **Terra MA3 LED** มีราคารีโมทเขียนไว้ **2 ที่** ต้องแก้ทั้งคู่ให้ตรงกัน:
> บรรทัด 613 `const accRemotePrice = hasRemote ? 1600 : 0;` และป้ายข้อความบรรทัด 258 `+1,600 ฿`

### 2.2 กันสาดตลับ / Terra FS (Cassette)

| สินค้า | เปิดไฟล์ | `Ctrl+F` หาคำว่า | แบบ |
|---|---|---|---|
| Terra FS Standard with LED | `terra-fs-full-cassette-standard-with-LED.html` | `PRICE_ROWS` | ⑤ |
| Terra FS Standard without LED | `terra-fs-standard-without-LED.html` | `PRICE_ROWS` | ⑤ |
| Terra FS Premium with LED | `terra-fs-full-cassette-premium-price-with-LED.html` | `PRICE_ROWS` | ⑤ |
| Terra FS Premium without LED | `terra-fs-premium-without-LED.html` | `PRICE_ROWS` | ⑤ |
| RV Awning | `rv-awning.html` | `PRICE_ROWS` | ⑤ |

### 2.3 กันสาดตั้ง / ม่านกันแดด (Vertical / Screen)

| สินค้า | เปิดไฟล์ | `Ctrl+F` หาคำว่า | แบบ | หมายเหตุ |
|---|---|---|---|---|
| SEATEL ST2000 | `seatel-st2000.html` | `PRICE_ROWS` | ⑤ | 56 แถว |
| SEATEL ST1000E | `seatel-st1000e-vertical-awning.html` | `const MODELS` | ② ⚠️ | บรรทัดเดียวยาวมาก |
| Izip Screen | `izip-screen.html` | `const table` | ⑨ | เรตต่อ ตร.ม. ตามช่วงพื้นที่ |
| ZIPGUIDE Expert | `zipguide-expert.html` | `PRICE_TABLES` | ⑧ ⚠️ | ตัวเลขล้วน ไม่มีป้ายกำกับ |
| ZIPGUIDE Ultra | `zipguide-ultra.html` | `PRICE_TABLES` | ⑧ ⚠️ | ตัวเลขล้วน ไม่มีป้ายกำกับ |

> ⚠️ **Izip Screen** มีราคา **พ่นสีฝุ่น (powder coat) 500 บาท/เมตร** เขียนไว้ **4 ที่**
> บรรทัด 152 (ป้าย) · 489 (สูตรคำนวณ) · 557 และ 597 (ข้อความส่งลูกค้า) — **ต้องแก้ให้ครบทั้ง 4**

### 2.4 สินค้าพิเศษ (Special Products)

| สินค้า | เปิดไฟล์ | `Ctrl+F` หาคำว่า | แบบ |
|---|---|---|---|
| Aland | `aland_retractable_arm_awning.html` | `PRICE_ROWS` | ⑤ |
| Veranda LT06 | `veranda-lt06.html` | `PRICE_ROWS` | ⑤ |
| Solidux Pergola | `solidux.html` | `PRICE_ROWS` | ⑤ |
| Canopy (โครงสร้างถาวร) | `canopy-calculator.html` | `PRICE_TABLE` | ⑨ |

### 2.5 เฉพาะโครงสร้าง (Structure Only) — 4 ไฟล์

ราคาโครงสร้างอยู่ใน **บรรทัดที่ 164** ของแต่ละไฟล์ ส่วนราคามอเตอร์และอุปกรณ์เสริมอ่านจาก `prices.js`

| สินค้า | เปิดไฟล์ | `Ctrl+F` หาคำว่า | แบบ |
|---|---|---|---|
| Spettmann Star — เฉพาะโครง | `spettmann-star-structure.html` | `"rows"` | ③ ⚠️ |
| Terra MA3 — เฉพาะโครง | `terra-ma3-structure.html` | `"rows"` | ③ ⚠️ |
| Terra MA4 — เฉพาะโครง | `ma4-calculator-structure.html` | `"rows"` | ③ ⚠️ |
| SEATEL ST2000 — เฉพาะโครง | `seatel-st2000-structure.html` | `"rows"` | ③ ⚠️ |

### 2.6 ผ้าใบ (Fabric Only)

| สินค้า | เปิดไฟล์ | `Ctrl+F` หาคำว่า | แบบ |
|---|---|---|---|
| ผ้าใบทุกชนิด (28 รายการ) | `fabric-price-calculator.html` | `FABRIC_PRICES` | ⑩ |
| ด้ายเย็บ | `fabric-price-calculator.html` | `const THREADS` | ⑩ |
| Solar Trim / ค่าติดตั้ง | `fabric-price-calculator.html` | `TRIM_PRICE_PER_YARD` | ⑩ |

### 2.7 iPergo — 25 ไฟล์ (Pergola / โรงจอดรถ)

ราคาอยู่ในบรรทัดที่ **~92** เป็น JSON บรรทัดเดียว → **แบบ ① ⚠️ อันตรายที่สุด อ่าน § 5.1 ก่อนแก้**
`Ctrl + F` พิมพ์ **`const PRODUCT`** (ใช้ได้ทั้ง 25 ไฟล์)

> ⚠️ **iPergo มี 2 แบบย่อย ดูให้ดีว่าไฟล์ที่เปิดเป็นแบบไหน**
> · **22 ไฟล์** ราคาเดียวต่อขนาด → เขียนว่า **`"price": 320600`**
> · **3 ไฟล์** (`ipergo-pergo-e` · `ipergo-m6-pro-cf` · `ipergo-m6-pro-cfw`) มี **2 ราคาต่อขนาด**
>   → เขียนว่า **`"prices": {"motor": 181900, "manual": 164200}`**
>   (`ipergo-pergo-e` ใช้ชื่อ `"manual"` กับ `"motorized_led"`)
> สังเกตง่าย ๆ: **`price`** (ไม่มี s) = ราคาเดียว · **`prices`** (มี s) = มีมือหมุน/มอเตอร์แยกกัน

**ชื่อรุ่น → ชื่อไฟล์** (ชื่อไฟล์เดาจากชื่อรุ่นไม่ได้ ต้องเปิดตารางนี้):

| รุ่น | ไฟล์ | | รุ่น | ไฟล์ |
|---|---|---|---|---|
| PERGO-E | `ipergo-pergo-e.html` | | PVC-F | `ipergo-pvc-f.html` |
| PERGO-R500 | `ipergo-r500.html` | | PVC-FD | `ipergo-pvc-fd.html` |
| DT-1S | `ipergo-dt-1s.html` | | PVC-FT | `ipergo-pvc-ft.html` |
| M6 Pro C-F | `ipergo-m6-pro-cf.html` | | PVC-FW | `ipergo-pvc-fw.html` |
| M6 Pro C-FW | `ipergo-m6-pro-cfw.html` | | PVC-FWD | `ipergo-pvc-fwd.html` |
| T600 C-F | `ipergo-t600-cf.html` | | PVC-FWT | `ipergo-pvc-fwt.html` |
| T600 C-FD | `ipergo-t600-cfd.html` | | PVC-WM | `ipergo-pvc-wm.html` |
| T600 C-FW | `ipergo-t600-cfw.html` | | PVC-WMD | `ipergo-pvc-wmd.html` |
| T600 C-FWD | `ipergo-t600-cfwd.html` | | PVC-WMT | `ipergo-pvc-wmt.html` |
| T600 C-WM | `ipergo-t600-cwm.html` | | PVC-WH | `ipergo-pvc-wh.html` |
| T600 C-WMD | `ipergo-t600-cwmd.html` | | PVC-WHD | `ipergo-pvc-whd.html` |
| T600 C-WH | `ipergo-t600-cwh.html` | | PVC-WHT | `ipergo-pvc-wht.html` |
| T600 C-WHD | `ipergo-t600-cwhd.html` | | | |

> 💡 **ไม่แน่ใจว่ารุ่นไหน?** เปิดเว็บจริง กดการ์ด iPergo ในหน้าแรก จะมีหน้าต่างให้เลือกรุ่นย่อย
> เลือกแล้วดูชื่อไฟล์บนแถบที่อยู่ (address bar) ของเบราว์เซอร์
> **F** = Freestanding (ตั้งอิสระ) · **W** = Wall Mounted (ติดผนัง) · **WH** = Wall Hanging (แขวนผนัง) ·
> **D** = Double (คู่) · **T** = Triple (สามช่อง)

### 2.8 ราคาที่ใช้ร่วมกันทุกรุ่น — แก้ที่ `prices.js` ที่เดียว

| ราคาอะไร | `Ctrl+F` หาคำว่า | บรรทัด | หมายเหตุ |
|---|---|---|---|
| อุปกรณ์เสริม Somfy (รีโมท เซ็นเซอร์ TaHoma) | `somfy: {` | 48–62 | |
| อุปกรณ์เสริม Strong Up | `strongUp: {` | 65–73 | |
| **ราคาตัวมอเตอร์ทุกรุ่น** | `motors: {` | 84–109 | ใช้ร่วม 8 ไฟล์ |
| **ฝาครอบกันน้ำ (Cover Profile)** | `coverProfile: {` | 123–126 | ⚠️ **บาทต่อความกว้าง 1 เมตร** |

**แก้ที่นี่ครั้งเดียว เปลี่ยนทุกไฟล์พร้อมกัน** — ทั้งตัวเลขที่ใช้คำนวณและตัวเลขบนป้ายตัวเลือก

> ⚠️ **ยกเว้น** `ma4-calculator.html`, `terra-ma3.html`, `spettmann-star.html`
> ราคามอเตอร์ของ 3 ไฟล์นี้ **รวมอยู่ในตารางราคาตามขนาดแล้ว** ต้องแก้ที่ตารางในไฟล์นั้นเอง

---

## 3. รู้จักไฟล์และเครื่องมือ

### 3.1 ไฟล์แต่ละชนิด

| ไฟล์ | คืออะไร |
|---|---|
| `ชื่อสินค้า.html` (49 ไฟล์) | เครื่องคำนวณ 1 ไฟล์ = 1 รุ่นสินค้า |
| `prices.js` | คลังราคากลาง — มอเตอร์ อุปกรณ์เสริม ฝาครอบ (ใช้ร่วมกันหลายรุ่น) |
| `sw.js` | ตัวคุมเวอร์ชันของเว็บ **ต้องเพิ่มเลขทุกครั้งที่แก้** |
| `index.html` | หน้าแรก รวมลิงก์เครื่องคำนวณทุกตัว |
| `backup.bat` | **ดับเบิลคลิก** เพื่อสำรองไฟล์ทั้งโฟลเดอร์ |
| `deploy.bat` | **ดับเบิลคลิก** เพื่อส่งขึ้นเว็บจริง |
| `login.bat` | **ดับเบิลคลิก** เพื่อเข้าสู่ระบบ Cloudflare (ทำครั้งเดียว) |
| `backup.ps1` `deploy.ps1` `login.ps1` | ตัวสั่งงานจริงของ 3 ไฟล์ข้างบน — **ห้ามลบ ห้ามย้าย ห้ามเปิดแก้** |

### 3.2 ติดตั้ง VS Code (ทำครั้งเดียว)

โหลดฟรีที่ **code.visualstudio.com** — จำเป็นเพราะ:
- เห็น **สีของโค้ด** ทำให้รู้ทันทีว่าพิมพ์ผิดตรงไหน (ตัวเลขต้องเป็นสีเดียวกันทั้งตาราง)
- มี **เลขบรรทัด** ด้านซ้าย ใช้กับตารางค้นหาในคู่มือนี้ได้เลย
- กด `Ctrl + Z` ย้อนกลับได้ไม่จำกัด

> **เปิดไฟล์ยังไง:** คลิกขวาที่ไฟล์ → **Open with Code**
> ถ้าไม่มีเมนูนี้ ให้เปิด VS Code ก่อน แล้วลากไฟล์เข้าไปวาง

### 3.3 สำรองไฟล์ก่อนแก้ — ดับเบิลคลิก `backup.bat`

จะได้โฟลเดอร์ชื่อ `Awning Calculator - backup 2026-08-03` อยู่ข้าง ๆ โฟลเดอร์งาน
ถ้าแก้พังเมื่อไหร่ ก๊อปไฟล์จากโฟลเดอร์นี้มาวางทับได้ทันที

---

## 4. ราคาเก็บอยู่ 4 ที่

| แบบ | ราคาอะไร | อยู่ที่ไหน |
|---|---|---|
| **A** | ราคาตามขนาด (กว้าง × ยื่น/ตก) | **ในไฟล์ HTML ของรุ่นนั้น** — รูปแบบต่างกัน ดู § 2 และ § 5 |
| **B** | อุปกรณ์เสริม (รีโมท เซ็นเซอร์) | `prices.js` บล็อก `somfy` / `strongUp` |
| **C** | ตัวมอเตอร์ | `prices.js` บล็อก `motors` |
| **D** | ฝาครอบกันน้ำ (Cover Profile) | `prices.js` บล็อก `coverProfile` |

### แบบ B, C — วิธีแก้ (ง่ายที่สุด)

เปิด `prices.js` → หาบรรทัด → พิมพ์ทับเฉพาะตัวเลข → `Ctrl + S`

```js
  strongUp: {
    remote1:      950,   // Transmitter 1 Channel / รีโมท 1 ช่อง   ← แก้ 950 เป็นราคาใหม่
    remote15:    1800,   // Transmitter 15 Channel / รีโมท 15 ช่อง
    windSun:     6600,   // Wind-Sun Sensor / เซ็นเซอร์ลม-แดด
```

> ⚠️ **ชื่อคีย์ (คำหน้า `:`) ต้องเป็นภาษาอังกฤษติดกัน ห้ามมีเว้นวรรค ห้ามภาษาไทย**
> เคยเกิดขึ้นจริง: ตั้งชื่อคีย์ว่า `สวิตซ์ Smoove` แล้ว **เครื่องคำนวณทุกตัวคำนวณอุปกรณ์เสริมไม่ได้พร้อมกันหมด**
> ถ้าอยากใส่คำอธิบายไทย ให้ใส่หลัง `//` เท่านั้น

### แบบ D — ฝาครอบกันน้ำ (ระวังหน่วย)

```js
  coverProfile: {
    aluminum: 2400,   // ฝาครอบอลูมิเนียม MF (สีเงิน) — บาท/เมตร
    white:    2900    // ฝาครอบสีขาว WH — บาท/เมตร
  }
```

> ⚠️ **หน่วยคือ "บาทต่อความกว้าง 1 เมตร"** ไม่ใช่ราคาต่อชุด
> ราคาจริงที่ลูกค้าจ่าย = ตัวเลขนี้ × ความกว้างงาน (เมตร)
> เช่น `2400` กับงานกว้าง 4 เมตร = 9,600 บาท
> ใช้ร่วมกัน **7 ไฟล์**: `seatel-st2000` · `seatel-st1000e-vertical-awning` · `chill-d1` ·
> `ma4-calculator` · `spettmann-star` · `terra-ma3` · `terra-ma3-LED`

### ราคามอเตอร์ปัจจุบัน (แบบ C)

```js
  motors: {
    strongUp50_12:           9900,   // Strong Up 50/12
    strongUp50_12_crank:     11900,  // Strong Up 50/12 + มือหมุน
    strongUp80_15:           15900,  // Strong Up 80/15
    somfyAltus50_12:         26800,  // Somfy Altus 50/12
    somfyAltus30_17:         24800,  // Somfy Altus 30/17 + Remote
    somfyAltus40_17:         26800,  // Somfy Altus 40/17 + Remote
    somfyAltus85_17:         36800,  // Somfy Altus 85/17 + Remote
    somfyMaestria15_17:      25000,  // Somfy Maestria 15/17 + Remote
    somfyMaestria35_17:      31000,  // Somfy Maestria 35/17 + Remote
    somfyMaestria50RTS15_17: 25000,  // MAESTRIA+50 RTS 15/17 + Remote
    somfyMaestria50RTS35_17: 31000,  // MAESTRIA+50 RTS 35/17 + Remote
    somfyAltus50RTS10_17:    21800,  // Altus 50 RTS 10/17 + Remote (เฉพาะ izip-screen)
    somfyAltus50RTS20_17:    23800,
    somfyAltus50RTS30_17:    24800,
    somfyAltus50RTS40_17:    25800
  }
```

> รุ่นที่ป้ายเขียนว่า **"+ Remote"** = ราคานี้**รวมรีโมท 1 ช่องมาแล้ว**
> เวลาเสนอราคาอย่าติ๊กรีโมท 1 ช่องเพิ่มอีก จะคิดซ้ำ
> **ตัวเลขข้างบนนี้เป็นค่าจริง ณ วันที่ปรับปรุงคู่มือ** ถ้าไม่ตรงกับในไฟล์ ให้ยึด `prices.js` เสมอ

---

## 5. รูปแบบตารางราคา 10 แบบ

แต่ละไฟล์เก็บราคาคนละแบบ ดูว่าไฟล์ที่จะแก้เป็นแบบไหนจาก § 2 แล้วอ่านเฉพาะแบบนั้น

---

### 5.1 ⚠️ แบบ ① — `PRODUCT` JSON บรรทัดเดียว (25 ไฟล์ iPergo)

**นี่คือแบบที่พังง่ายที่สุด — อ่านให้จบก่อนแตะ**

ราคาทั้งหมดอยู่ใน **บรรทัดเดียวที่ยาวหลายหมื่นตัวอักษร** (บรรทัดที่ ~92)

**แบบย่อย ก — ราคาเดียวต่อขนาด (22 ไฟล์)**

```js
const PRODUCT = {"filename": "ipergo-pvc-f.html", "title": "iPergo PVC-F", ... "items": [
{"id": "2-5-2-5", "label": "Length 2.5 m. x Projection 2.5 m.", "price": 320600, ...},
{"id": "3-0-2-5", "label": "Length 3.0 m. x Projection 2.5 m.", "price": 356200, ...}, ...
```

**แบบย่อย ข — มือหมุน/มอเตอร์แยกกัน (3 ไฟล์: `pergo-e` · `m6-pro-cf` · `m6-pro-cfw`)**

```js
{"id": "2-5-2-5", "label": "Length 2.5 m. x Projection 2.5 m.", ...,
 "prices": {"motor": 181900, "manual": 164200}},
```
> `ipergo-pergo-e.html` ใช้ชื่อ `"manual"` กับ `"motorized_led"` แทน
> ⚠️ **ต้องแก้ให้ครบทั้งสองตัวเลข** ถ้าราคาขึ้นทั้งมือหมุนและมอเตอร์

*(ในไฟล์จริงทั้งหมดนี้อยู่บรรทัดเดียวติดกัน ไม่มีการขึ้นบรรทัดใหม่)*

**ขั้นตอนที่ปลอดภัย:**

1. เปิดไฟล์ใน VS Code แล้วกด **`Alt + Z`** ก่อนเสมอ — บรรทัดยาวจะพับลงมาให้อ่านได้
2. กด `Ctrl + F` พิมพ์ **ราคาเดิม** ที่ต้องการแก้ (เช่น `320600`) แทนการไล่หาด้วยตา
3. ตรวจว่าถูกตัวจริง โดยดู `"label"` ที่อยู่**ก่อนหน้า** ราคานั้น — บอกขนาดไว้ชัดเจน
4. พิมพ์ทับเฉพาะตัวเลข

> ❌ **ห้ามกด Enter ตัดบรรทัด** ไม่ว่ากรณีใด — ไฟล์จะพังทันที
> ❌ **ห้ามกด `Ctrl + H` แทนที่ทั้งหมด (Replace All)** เพราะตัวเลขเดียวกันอาจโผล่หลายที่
> ✅ ถ้าเผลอกด Enter → กด `Ctrl + Z` ทันที

---

### 5.2 ⚠️ แบบ ② — `MODELS` JSON บรรทัดเดียวแบบซ้อน (`seatel-st1000e-vertical-awning.html`)

อยู่บรรทัด 152 หลักการแก้เหมือนแบบ ① ทุกอย่าง (กด `Alt + Z` ก่อน · ค้นหาราคาเดิม · ห้ามกด Enter)
ต่างกันที่โครงสร้างซ้อน 3 ชั้น: **รุ่นผ้า → ช่วงตก → ช่วงกว้าง → ชนิดผ้า**

```js
const MODELS = {"vinistar": {..., "table": {"0.10-1.00": {"1.00-1.50": {"Vinistar 0.75 mm": 11100, ...
```

> ไฟล์นี้ยังมี **ราคาสำรอง** เขียนไว้ในบรรทัด 99–111 เป็น `data-price="..."`
> ถ้าแก้ราคาอุปกรณ์เสริมใน `prices.js` แล้ว **ควรมาแก้ตัวเลขพวกนี้ให้ตรงกันด้วย** (ดู § 11.2)

---

### 5.3 ⚠️ แบบ ③ — `const P = {...}` บรรทัดเดียว (4 ไฟล์ `*-structure.html`)

อยู่ **บรรทัดที่ 164** ของทั้ง 4 ไฟล์ กด `Alt + Z` ก่อนเช่นกัน

```js
const P = {"label": "MA4 Compact Folding Arm Awning", ...,
  "widths": [[2.0,2.5],[2.51,3.0],[3.01,3.5],[3.51,4.0],[4.01,4.5],[4.51,5.0]],
  "rows": [
    {"key":1.5, "label":"1.50 เมตร", "prices":[5540, 5820, 6130, 6750, 7484, 8111]},
    {"key":2.0, "label":"2.00 เมตร", "prices":[null, 6250, 6560, 7180, 7913, 8540]}, ...
```

**อ่านยังไง:**
- `"widths"` = ช่วงความกว้าง เรียงซ้ายไปขวา (คู่แรก `[2.0, 2.5]` = 2.00–2.50 ม.)
- แต่ละ `"rows"` = **ระยะยื่น 1 ค่า** (ดูที่ `"label"`)
- ตัวเลขใน `"prices"` เรียงตรงกับ `"widths"` **ตำแหน่งต่อตำแหน่ง**

> **ตัวอย่าง:** ระยะยื่น 1.50 ม. × กว้าง 3.01–3.50 ม. → แถว `"1.50 เมตร"` ตัวเลข**ตัวที่ 3** = `6130`
> ⚠️ **จำนวนตัวเลขใน `prices` ต้องเท่ากับจำนวนช่วงใน `widths` เสมอ** (ที่นี่คือ 6 ตัว)
> ห้ามเพิ่มหรือลดจำนวนตัวเลข ถ้าช่องไหนขายไม่ได้ให้ใส่ `null`

---

### 5.4 แบบ ④ — `PRICE_MANUAL_*` / `PRICE_MOTOR_*` (Terra MA3, MA3 LED, Spettmann Star)

ชื่อตารางบอกทุกอย่าง: `PRICE_` + **ระบบ** + `_` + **ชนิดผ้า**

- `PRICE_MANUAL_...` = ระบบ **มือหมุน**
- `PRICE_MOTOR_...` = ระบบ **มอเตอร์**
- ท้ายชื่อ = ชนิดผ้า `_ACRYLIC` `_SOLAR` `_SUNBRELLA` `_NOVA`

```js
const PRICE_MANUAL_SUNBRELLA = {
  "1.50": [        // ← ระยะยื่น 1.50 เมตร
    25350, // 2.00–2.50   ← ความกว้าง (คำอธิบายหลัง // บอกไว้ให้แล้ว)
    27780, // 2.51–3.00
    30270, // 3.01–3.50
  ],
  "2.00": [        // ← ระยะยื่น 2.00 เมตร
    null,  // 2.00–2.50 ไม่มี   ← null = ขายไม่ได้ (ในตาราง PDF เป็น x)
    31130, // 2.51–3.00
```

**แก้:** หาบรรทัดที่ตรงกับขนาด → พิมพ์ทับตัวเลข → **คงลูกน้ำท้ายบรรทัดไว้**

```js
    25350, // 2.00–2.50      ← เดิม
    26100, // 2.00–2.50      ← ใหม่ (คง , ไว้)
```

> **แบบนี้อ่านง่ายที่สุด** เพราะมีคำอธิบายช่วงความกว้างกำกับทุกบรรทัด
> ในตาราง PDF ช่องไหนเป็น **x** → ในโค้ดใช้ **`null`** (อย่าใส่ `0` เพราะจะกลายเป็นขายฟรี)

---

### 5.5 แบบ ⑤ — `PRICE_ROWS` (9 ไฟล์: Terra FS ×4, RV, SEATEL ST2000, Aland, Veranda, Solidux)

แบบที่ใช้เยอะที่สุด **1 แถว = 1 ช่วงขนาด · คอลัมน์ = ชนิดผ้า**

```js
const PRICE_ROWS = [
  { "width_band": "1.00-1.50",     // ← ช่วงความกว้าง
    "drop_band": "0.10-1.00",      // ← ช่วงความตก/ยื่น
    "Solar View": 12300,           // ← ชนิดผ้า : ราคา
    "Solar Screen": 12700,
    "Sun Text": 12900,
    "Solar Zip 5%": 12700,
    "Vinista Clear 1.00 mm": 13500 },
  { "width_band": "1.51-2.00",
    ...
```

**แก้:** หาแถวที่ `width_band` + `drop_band` ตรงกับที่ต้องการ → หาชื่อผ้า → พิมพ์ทับตัวเลข

> ⚠️ **ชื่อผ้าในเครื่องหมายคำพูดต้องสะกดตรงเป๊ะทุกแถว** รวมทั้งเว้นวรรคและตัวพิมพ์ใหญ่-เล็ก
> ถ้าพิมพ์ผิดแม้แต่ตัวเดียว ผ้าชนิดนั้นจะคำนวณไม่ได้ (ขึ้น `NaN` หรือ 0)
> 💡 **ขึ้นราคาทั้งตาราง?** อย่าไล่แก้มือทีละช่อง — บอกทีมพัฒนาให้คำนวณให้ ปลอดภัยกว่ามาก

---

### 5.6 แบบ ⑥ — `PRICE_MATRIX` ซ้อน 3 ชั้น (`chill-d1.html`)

**กว้าง → ตก → ชนิดผ้า**

```js
const PRICE_MATRIX = {
  "1.00-1.50": {                    // ← ช่วงความกว้าง
    "0.80-1.20": { pvc: 10900, view: 11430, acrylic: 12870,
                   sunbrella: 13770, solarScreen: 11900, suntext: 12060 },
    "1.21-1.40": { pvc: 11330, view: 11960, ... },
    "1.41-1.80": { pvc: 12160, view: 12970, ... }
  },
  "1.51-2.00": {                    // ← ช่วงความกว้างถัดไป
    "0.80-1.20": { pvc: 11610, ... },
```

**แก้:** ไล่จากช่วงกว้าง → ช่วงตก → ชื่อผ้า → พิมพ์ทับตัวเลข

---

### 5.7 แบบ ⑦ — `PRICE_TABLES` ผ้า → มือหมุน/มอเตอร์ (`ma4-calculator.html`)

```js
const WIDTH_BANDS = [               // ← ช่วงความกว้าง เรียงซ้ายไปขวา
  { min:2.00, max:2.50, label:'2.00-2.50' },
  { min:2.51, max:3.00, label:'2.51-3.00' }, ...
];

const PRICE_TABLES = {
  nova: {                                        // ← ชนิดผ้า
    label: 'Solar Nova / Outdoor Acrylic Fabric',
    manual: {                                    // ← ระบบมือหมุน
      '1.50':[16500, 18000, 19600, 21900, 24400, 26700],   // ← ระยะยื่น 1.50 ม.
      '2.00':[null,  20400, 22300, 24800, 27500, 30100],
    },
    motor: {                                     // ← ระบบมอเตอร์
      '1.50':[26400, 27900, 29500, 31800, 34300, 36600],
```

ตัวเลขในวงเล็บ `[ ]` เรียงตรงกับ `WIDTH_BANDS` **ตำแหน่งต่อตำแหน่ง** (6 ช่อง)

> ⚠️ ไฟล์นี้ **ราคามอเตอร์รวมอยู่ในตารางแล้ว** ไม่ได้อ่านจาก `prices.js`
> ถ้าราคามอเตอร์เปลี่ยน ต้องแก้ตัวเลขในบล็อก `motor` ของทุกชนิดผ้าเอง

---

### 5.8 ⚠️ แบบ ⑧ — `PRICE_TABLES` ตัวเลขล้วน (`zipguide-expert.html`, `zipguide-ultra.html`)

**แบบนี้ไม่มีป้ายกำกับเลย ต้องนับตำแหน่งเอา — ระวังเป็นพิเศษ**

```js
const WIDTH_RANGES = ["0.80-1.00","1.01-1.50","1.51-2.00", ... ];  // 10 ช่วง
const DROP_RANGES  = ["0.50-1.00","1.01-1.50","1.51-2.00", ... ];  //  8 ช่วง

const PRICE_TABLES = {
  prosense: [                                            // ← ระบบมอเตอร์
    [[41470,46250],[47850,51040],[51040,55820], ... ],   // ← แถวที่ 1 = ตก 0.50-1.00
    [[44660,47850],[51040,52630],[54230,59010], ... ],   // ← แถวที่ 2 = ตก 1.01-1.50
  ],
  somfy: [ ... ]
};
```

**อ่านยังไง (3 ชั้น):**

| ชั้น | คืออะไร | อ้างอิงจาก |
|---|---|---|
| แถวนอก | **ช่วงความตก** | `DROP_RANGES` ตามลำดับ |
| กลุ่มในแถว | **ช่วงความกว้าง** | `WIDTH_RANGES` ตามลำดับ |
| ตัวเลขในคู่ `[a,b]` | **ชนิดผ้า** | `a` = ผ้า **5%** · `b` = ผ้า **1% และ 0%** |

> **ตัวอย่าง:** ตก 0.50–1.00 × กว้าง 1.01–1.50 × ผ้า 5%
> → แถวแรก → กลุ่มที่ 2 `[47850, 51040]` → ตัวแรก = **47,850**
> ⚠️ **ห้ามเพิ่ม/ลดจำนวนวงเล็บหรือจำนวนตัวเลข** — ทุกแถวต้องมี 10 กลุ่ม แต่ละกลุ่มมี 2 ตัว
> 💡 นับตำแหน่งด้วยนิ้วบนจอ หรือให้เพื่อนช่วยนับซ้ำ ก่อนพิมพ์ทับ

---

### 5.9 แบบ ⑨ — ราคาต่อตารางเมตร (`canopy-calculator.html`, `izip-screen.html`)

**Canopy** — ชนิดผ้า × ชนิดโครงสร้าง (บาท/ตร.ม.)

```js
const PRICE_TABLE = {
  pvc:       { steel_normal:2900, steel_euro:3300, stainless_normal:3900,
               stainless_euro:4200, aluminum_fan:4000, label:'ผ้า PVC, ผ้า Solar...' },
  solarmax:  { steel_normal:3100, steel_euro:3500, ... },
  acrylic:   { steel_normal:3700, ... },
  sunbrella: { steel_normal:4000, ... }
};
```

> `label:` เป็น **ข้อความ ไม่ใช่ราคา** — อย่าไปแก้ตัวเลขในนั้น

**Izip Screen** — ยิ่งพื้นที่มาก ราคาต่อ ตร.ม. ยิ่งถูกลง

```js
const table = [
  {range:[0,3],       f0:7700, f1:7500, f5:7300},   // ← พื้นที่ 0–3 ตร.ม.
  {range:[3.01,4],    f0:6300, f1:6200, f5:6000},
  {range:[10.01,999], f0:4500, f1:4200, f5:4000}    // ← 10 ตร.ม. ขึ้นไป
];
```

| ช่อง | ความหมาย |
|---|---|
| `range` | ช่วงพื้นที่ (ตร.ม.) |
| `f0` / `f1` / `f5` | ราคาต่อ ตร.ม. ของผ้าทึบ **0%** / **1%** / **5%** |

> ⚠️ **ช่วง `range` ต้องต่อกันไม่ให้มีรู** เช่น แถวก่อนจบที่ `3` แถวถัดไปต้องเริ่มที่ `3.01`
> ถ้าเว้นช่องว่างไว้ งานที่ตกในรูนั้นจะคิดราคาได้ **0 บาท**

---

### 5.10 แบบ ⑩ — `FABRIC_PRICES` (`fabric-price-calculator.html`)

**อ่านง่ายที่สุด — 1 บรรทัด = ผ้า 1 ชนิด**

```js
const FABRIC_PRICES = [
  { key:'solar_max', group:'Solar Series',
    label:'Solar Max - ผ้าใบ Coated PVC Sioen B8000 630 g',
    roll:2.50, len:60, unit:'sqm', price:350, a:550, b:650, c:900 },
```

| ช่อง | ความหมาย |
|---|---|
| `roll` | **หน้ากว้างผ้ามาตรฐาน** (เมตร) — เป็นแค่**ค่าตั้งต้น** ที่เด้งไปใส่ในช่อง "หน้ากว้างผ้า" ผู้ใช้พิมพ์ทับได้ ถ้าใส่ผิดพื้นที่จะคิดผิดทั้งหมด |
| `len` | ความยาวต่อม้วน (เมตร) — แสดงเป็นข้อมูลเฉย ๆ ไม่ได้ใช้คำนวณ |
| `unit` | หน่วยของ `price` **เท่านั้น**: `'m'` = ต่อเมตร · `'sqm'` = ต่อตารางเมตร |
| `price` | **ราคาเฉพาะผ้า** (ยังไม่ตัดเย็บ) ตามหน่วย `unit` |
| `a` `b` `c` | **ราคาตัดเย็บ** Price A / B / C หน่วยเป็น **บาท/ตร.ม. เสมอ** ไม่ว่า `unit` จะเป็นอะไร |

> ⚠️ **ห้ามสลับ `unit:'m'` กับ `unit:'sqm'`** เพราะคิดเงินคนละแบบ
> (ต่อเมตร = ความยาว × ราคา · ต่อ ตร.ม. = พื้นที่ × ราคา)
> ให้ยึดตามคอลัมน์ "ราคาเฉพาะผ้า/หน่วย" ในตารางราคาต้นฉบับ

**ราคาอื่นในไฟล์เดียวกัน** (อยู่ถัดลงมา):

```js
const THREADS = [                                    // ราคาด้ายต่อหลอด
  { key:'dabond', label:'DABOND (2,000 เมตร/หลอด)', price:600 }, ...
];
const TRIM_PRICE_PER_YARD = 15;    // Solar Trim ผ้ากันขอบ — บาท/หลา
const INSTALL_RATE_PER_SQM = 300;  // ค่าขนส่ง+ติดตั้ง (เปลี่ยนผ้าใช้โครงเดิม) — บาท/ตร.ม.
const INSTALL_MINIMUM = 3000;      // ค่าบริการขั้นต่ำ — บาท
```

> ไฟล์นี้ **ไม่ได้ใช้ `prices.js`** — ราคาผ้าทั้งหมดแก้ที่ไฟล์นี้ที่เดียว

---

## 6. กรณีพิเศษที่ควรรู้

### 6.1 เครื่องคำนวณผ้าใบ — หน้ากว้างผ้าและการต่อผ้า

ช่อง **"การต่อผ้า / Joining Pattern"** เลือกได้ว่าจะเอา**ด้านไหนของงาน**ไปหารด้วยหน้ากว้างผ้า

| ตัวเลือก | หน้าผ้าพาดตาม | จำนวนท่อน |
|---|---|---|
| **ต่อแนวตั้ง** (ค่าตั้งต้น) | ความกว้างงาน | ความกว้าง ÷ หน้ากว้างผ้า |
| **ต่อแนวนอน** | ความยาวงาน | ความยาว ÷ หน้ากว้างผ้า |

> 💡 **สองแบบนี้ราคาไม่เท่ากัน — ลองสลับดูก่อนเสนอราคาทุกครั้ง**
> งาน 3.00 × 5.00 ม. บนผ้าหน้ากว้าง 2.50 ม.: แนวตั้ง = **25.00 ตร.ม.** · แนวนอน = **15.00 ตร.ม.**
> เครื่องคำนวณ **ไม่เลือกแบบที่ถูกที่สุดให้อัตโนมัติ** ต้องสลับดูเอง

ช่อง **"ระยะทับกันของรอยต่อ / Seam Overlap (ซม.)"** = ผ้าส่วนที่ต้องวางทับกันก่อนเย็บ
ค่าตั้งต้น `0` ถ้าร้านมีมาตรฐานประจำ (เช่น 5 ซม.) เปลี่ยนค่าตั้งต้นได้ที่
`<input id="seamOverlap" ... value="0"/>` ในไฟล์

> ⚠️ **ระยะทับมีผลกับ "จำนวนท่อน" ไม่ใช่แค่พื้นที่**
> ผ้า 2.50 ม. ทับกัน 5 ซม. → 2 ท่อนคลุมได้แค่ **4.95 ม.** ไม่ใช่ 5.00 ม.
> งานกว้าง 5.00 ม. จึงต้องใช้ **3 ท่อน** ราคากระโดดจาก 15.00 → 22.50 ตร.ม.

**รูปผังการตัดต่อผ้า** เหนือข้อความส่งลูกค้า วาดตามสเกลจริง:
กรอบ**ฟ้า** = งานจริง · แถบ**ส้ม** = ผ้าที่จมในตะเข็บ · พื้นที่**ลายแดง** = เศษเจียนทิ้ง · เส้น**ประน้ำเงิน** = รอยเย็บ
รูปนี้ **ไม่ติดไปกับข้อความที่กดคัดลอกส่งลูกค้า** เป็นภาพช่วยดูเฉย ๆ

> ⚠️ ระบบ**ยังไม่เตือน**กรณีผ้าที่ต้องใช้ยาวเกิน 1 ม้วน (`len`) — ต้องเช็กเอง

### 6.2 ไฟล์เฉพาะโครงสร้าง — ราคามาจาก 2 ที่

ทั้ง 4 ไฟล์ `*-structure.html` ผสมกัน:

| ราคาอะไร | อยู่ที่ไหน |
|---|---|
| **ราคาโครงสร้าง** | บรรทัด 164 ของไฟล์นั้นเอง (แบบ ③) |
| **ราคามอเตอร์** | `prices.js` บล็อก `motors` |
| **อุปกรณ์เสริม** | `prices.js` บล็อก `somfy` / `strongUp` |

---

## 7. ⭐ ขั้นตอนสำคัญที่สุด — เพิ่มเลขเวอร์ชันใน `sw.js`

**ถ้าข้ามขั้นตอนนี้ ลูกค้าจะยังเห็นราคาเก่า!** เพราะเว็บถูกเก็บไว้ในเครื่องลูกค้า (PWA/offline)

1. เปิด `sw.js`
2. หาบรรทัดที่ **13** (อยู่ต้น ๆ ไฟล์)
3. เพิ่มเลขท้ายขึ้น **1**

```js
const CACHE_NAME = 'sangthong-awning-v23';   // ← ของเดิม
const CACHE_NAME = 'sangthong-awning-v24';   // ← แก้เป็นเลขถัดไป
```

4. `Ctrl + S`

> ⚠️ **ห้ามลดเลข ห้ามใช้เลขซ้ำ** — เลขในไฟล์ตอนนี้เป็นเท่าไหร่ ให้ +1 จากนั้นเสมอ
> อย่ายึดตัวเลขตัวอย่างในคู่มือนี้ ให้เปิดไฟล์ดูของจริงทุกครั้ง

**ถ้าเพิ่มหน้าเครื่องคำนวณใหม่** (ไม่ใช่แค่แก้ราคา) ต้องเพิ่มชื่อไฟล์ในรายการ `ASSETS`
(บรรทัด 15–71) ด้วย ไม่งั้นหน้านั้นจะใช้แบบออฟไลน์ไม่ได้ — งานนี้ควรให้ทีมพัฒนาทำ

---

## 8. ทดสอบก่อนเผยแพร่

1. ดับเบิลคลิกไฟล์ที่แก้ (เช่น `terra-ma3.html`) เปิดในเบราว์เซอร์
2. เลือกขนาด/ผ้า/อุปกรณ์ **ที่ตรงกับราคาที่เพิ่งแก้**
3. เช็กว่าราคาที่แสดง **ตรงกับที่ตั้งใจไว้**
4. เช็กว่าไม่มีคำว่า **`NaN`** · **`undefined`** · หรือช่องราคาว่างเปล่า
5. **ลองขนาดข้างเคียงอีก 1–2 ขนาด** เผื่อเผลอแก้ผิดช่อง
6. ถ้ายังเห็นราคาเก่า กด **`Ctrl + Shift + R`**

> ถ้าหน้าจอเพี้ยนหรือราคาไม่ขึ้นเลย = พิมพ์ผิด (ลบ `,` หรือ `]` ไป) → ดู § 11

---

## 9. ⭐ เผยแพร่ขึ้นเว็บจริง — ดับเบิลคลิก `deploy.bat`

> เว็บจริงอยู่ที่ **https://awning-calculator.pages.dev**
> ⚠️ การแก้ไฟล์ในเครื่องหรือส่งขึ้น GitHub เฉย ๆ **ไม่ทำให้เว็บจริงเปลี่ยน** ต้อง deploy เท่านั้น

### 9.1 ขั้นตอน

1. **ดับเบิลคลิก `deploy.bat`** ในโฟลเดอร์งาน
2. หน้าต่างสีดำจะเปิดขึ้น **แสดงเลขเวอร์ชันใน `sw.js` ให้ดูก่อน** — เช็กว่าเพิ่มเลขแล้วจริง
   จากนั้นพิมพ์ **`Y`** แล้วกด Enter เพื่อยืนยัน (ถ้ากด Enter เฉย ๆ = ยกเลิก ไม่มีอะไรเกิดขึ้น)
3. รอสักครู่ จนขึ้นข้อความ **`Deployment complete!`**
4. **ห้ามปิดหน้าต่างระหว่างรอ** — ถ้าปิดกลางคัน เว็บอาจอัปเดตไม่ครบ
5. เปิด **https://awning-calculator.pages.dev** กด `Ctrl + Shift + R` แล้วเช็กราคา

### 9.2 ครั้งแรกสุดต้อง login ก่อน (ทำครั้งเดียวตลอดไป)

ถ้าหน้าต่างขึ้นว่ายังไม่ได้เข้าสู่ระบบ ให้ดับเบิลคลิก **`login.bat`**
เบราว์เซอร์จะเปิดให้กด **Allow** (บัญชี Cloudflare: pteerapr@gmail.com) แล้วค่อยรัน `deploy.bat` ใหม่

### 9.3 ยังเห็นราคาเก่าอยู่?

1. กด **`Ctrl + Shift + R`** (โหลดใหม่แบบล้างแคช)
2. ถ้ายังไม่หาย: กด `F12` → แท็บ **Application** → **Storage** → **Clear site data** → รีโหลด
3. ถ้ายังไม่หายอีก: **เช็กว่าลืมเพิ่มเลขใน `sw.js` หรือเปล่า** (§ 7) — สาเหตุที่พบบ่อยที่สุด

### 9.4 ถ้าอยากพิมพ์คำสั่งเอง

เปิด Terminal ในโฟลเดอร์โปรเจกต์ (VS Code → เมนู **Terminal → New Terminal**) แล้วพิมพ์:

```bash
npx wrangler pages deploy . --project-name awning-calculator --branch main --commit-dirty=true
```

---

## 10. ⭐ ถ้าทำพัง — ย้อนกลับได้ 3 ระดับ

**ใจเย็น ๆ ทุกกรณีย้อนกลับได้** เลือกตามสถานการณ์:

| สถานการณ์ | วิธีแก้ | เวลาที่ใช้ |
|---|---|---|
| **1. ยังไม่ได้กด `Ctrl + S`** | กด **`Ctrl + Z`** ใน VS Code ย้อนได้เรื่อย ๆ จนกว่าจะกลับเป็นเดิม | ทันที |
| **2. เซฟแล้ว แต่ยังไม่ได้ deploy** | เปิดโฟลเดอร์ `Awning Calculator - backup วันที่...` ที่ `backup.bat` สร้างไว้ → ก๊อปไฟล์ที่พังมาวางทับ | 1 นาที |
| **3. deploy ขึ้นเว็บจริงไปแล้ว** | เข้า **dash.cloudflare.com** → **Workers & Pages** → **awning-calculator** → แท็บ **Deployments** → หาตัวก่อนหน้าที่ยังดีอยู่ → กดปุ่ม **⋯** → **Rollback to this deployment** | 2 นาที |

> ⭐ **ระดับ 3 คือทางออกที่ปลอดภัยที่สุด** — เว็บจริงกลับไปเป็นเวอร์ชันก่อนหน้าทันที
> **โดยไม่ต้องแตะโค้ดเลย** ค่อย ๆ มาแก้ไฟล์ในเครื่องให้ถูกทีหลังได้สบาย ๆ

---

## 11. แก้ปัญหาที่พบบ่อย

### 11.1 ตารางอาการ → วิธีแก้

| อาการ | สาเหตุที่พบบ่อย | วิธีแก้ |
|---|---|---|
| ราคาไม่ขึ้น / หน้าเพี้ยนทั้งหน้า | ลบ `,` `]` `}` `"` ไปโดยไม่ตั้งใจ | `Ctrl + Z` หรือเอาไฟล์สำรองมาเทียบ |
| ราคาเพี้ยนมาก (เหลือหลักสิบ) | ใส่ลูกน้ำในตัวเลข เช่น `26,100` | ลบลูกน้ำออก เหลือ `26100` |
| **เครื่องคำนวณทุกตัวพังพร้อมกัน** | `prices.js` พัง (ชื่อคีย์มีเว้นวรรค/ภาษาไทย) | แก้ชื่อคีย์เป็นอังกฤษติดกัน (§ 4) |
| แก้แล้วแต่ยังเห็นราคาเก่า | ลืมเพิ่มเลข v ใน `sw.js` | เพิ่มเลข (§ 7) แล้ว deploy ใหม่ |
| เบราว์เซอร์ตัวเองยังเห็นเก่า | แคชในเครื่อง | `Ctrl + Shift + R` |
| ช่องที่ควร "ไม่มี" กลับมีราคา 0 | ใส่ `0` แทน `null` | เปลี่ยนเป็น `null` |
| ราคาผ้าบางชนิดเป็น `NaN` | สะกดชื่อผ้าในตารางไม่ตรงกัน (แบบ ⑤) | เทียบชื่อผ้ากับแถวอื่นให้ตรงเป๊ะ |
| ผ้าใบคิดพื้นที่ผิดมาก | ช่อง "หน้ากว้างผ้า" ถูกพิมพ์ทับ | กดปุ่ม **รีเซ็ต** ใต้ช่อง |

### 11.2 ⚠️ จุดที่ต้องแก้หลายที่ (รู้ไว้ก่อนจะได้ไม่ตกหล่น)

ราคาบางตัวถูกเขียนซ้ำไว้หลายจุด — **ถ้าแก้ไม่ครบจะเพี้ยนแบบเงียบ ๆ**

| ราคา | ต้องแก้ที่ |
|---|---|
| พ่นสีฝุ่น Izip Screen (500 บาท/ม.) | `izip-screen.html` บรรทัด **152 · 489 · 557 · 597** |
| รีโมท Terra MA3 LED (1,600 บาท) | `terra-ma3-LED.html` บรรทัด **258 · 613** |
| ราคาสำรองมอเตอร์ | `*-structure.html` ทั้ง 4 ไฟล์ บรรทัด **175–179** (ตัวเลขหลัง `\|\|`) |
| ราคาสำรองอุปกรณ์เสริม | `seatel-st1000e-vertical-awning.html` บรรทัด **99–111** (`data-price="..."`) |
| ค่าติดตั้ง 300 บาท/ตร.ม. + ขั้นต่ำ 3,000 บาท | `fabric-price-calculator.html` บรรทัด **229 · 227** (ตัวเลขที่ใช้คำนวณ) และ **140** (ข้อความบนหน้าจอ) |
| ข้อความเงื่อนไขค่าขนส่ง | `fabric-price-calculator.html` บรรทัด **609 · 634** (ไทย/อังกฤษ) |

> 💡 **"ราคาสำรอง" คืออะไร:** เป็นตัวเลขที่ระบบใช้เผื่อกรณี `prices.js` โหลดไม่สำเร็จ
> ปกติจะไม่ถูกใช้ แต่ถ้าค้างเลขเก่าไว้ วันที่เน็ตมีปัญหาจะคิดราคาผิดโดยไม่มีใครรู้
> **แจ้งทีมพัฒนาให้เช็กตัวเลขพวกนี้ทุกครั้งที่แก้ราคามอเตอร์หรืออุปกรณ์เสริม**

### 11.3 เช็กลิสต์ก่อนส่งงานทุกครั้ง ✅

- [ ] สำรองไฟล์แล้ว (`backup.bat`)
- [ ] แก้ตัวเลขในไฟล์ที่ถูกต้อง (เช็กกับตารางค้นหา § 2)
- [ ] ตัวเลขไม่มีลูกน้ำ (`26100` ไม่ใช่ `26,100`)
- [ ] เครื่องหมาย `,` `[ ]` `{ }` `"` ครบเหมือนเดิม
- [ ] ช่องที่ขายไม่ได้ใช้ `null` ไม่ใช่ `0`
- [ ] เช็กจุดที่ต้องแก้หลายที่แล้ว (§ 11.2)
- [ ] **เพิ่มเลขเวอร์ชันใน `sw.js` แล้ว**
- [ ] เปิดทดสอบในเบราว์เซอร์ ราคาถูกต้อง ไม่มี `NaN`/`undefined`
- [ ] **deploy ขึ้น Cloudflare แล้ว** และเปิดเว็บจริงเช็กซ้ำแล้ว
- [ ] บันทึกสิ่งที่แก้ลงใน Changelog (§ 12)

---

## 12. บันทึกการเปลี่ยนแปลง (Changelog)

> ทุกครั้งที่มีการแก้ราคา หรือเพิ่ม/เปลี่ยนฟีเจอร์ ให้บันทึกสั้น ๆ ไว้ที่นี่ (วันที่ · สิ่งที่เปลี่ยน · ไฟล์ที่เกี่ยวข้อง) จะได้ย้อนดูได้ว่าใครแก้อะไรเมื่อไหร่

- **3 ส.ค. 2026** — **แก้ราคาสำรองของ Somfy Altus 85/17 ที่ค้างเลขเก่า `36,500` ให้ตรงกับ `prices.js` (`36,800`)** รวม **5 จุด**: ไฟล์เฉพาะโครงสร้างทั้ง 4 ไฟล์ (`ma4-calculator-structure` · `seatel-st2000-structure` · `spettmann-star-structure` · `terra-ma3-structure`) บรรทัด 176 ตัวเลขหลัง `||` · และ `seatel-st1000e-vertical-awning.html` บรรทัด 90 ทั้งค่า `value="36500"` และข้อความบนป้าย `(36,500 บาท)`. **หมายเหตุ:** เลข `36500` ที่เหลืออยู่ในบรรทัด 152 ของ `seatel-st1000e` เป็น **ราคาผ้า Solar Screen ช่วง 4.51-5.00 ม.** ไม่ใช่ราคามอเตอร์ จึงไม่ได้แก้. **และแก้ราคาสำรองมอเตอร์ Somfy ที่เหลืออีก 4 รุ่น** ใน `seatel-st1000e-vertical-awning.html` บรรทัด 88-92 ให้ตรงกับ `prices.js` ทั้งค่า `value` และตัวเลขบนป้าย: Altus 30/17 `24,500 → 24,800` · Altus 40/17 `26,500 → 26,800` · Maestria 15/17 `24,500 → 25,000` · Maestria 35/17 `30,500 → 31,000`. **ตรวจสอบแล้วว่าตอนนี้ราคามอเตอร์ทั้ง 10 รุ่นและอุปกรณ์เสริมทั้ง 9 รายการในไฟล์นี้ตรงกับ `prices.js` ครบทุกตัว**
- **3 ส.ค. 2026** — **เขียนคู่มือเล่มนี้ใหม่ทั้งฉบับให้ครอบคลุมเครื่องคำนวณครบทั้ง 49 ตัว** (เดิมครอบคลุมประมาณ 13 ตัว): เพิ่ม **§ 2 ตารางค้นหา "สินค้า → ไฟล์ → คำที่ต้องค้นหา"** ครบทุกรุ่นรวมทั้ง iPergo ทั้ง 25 ไฟล์ที่คู่มือเดิมไม่เคยพูดถึงเลย · เพิ่ม **§ 5 อธิบายรูปแบบตารางราคาครบทั้ง 10 แบบ** (เดิมมีแค่แบบ `PRICE_MANUAL_*` แบบเดียว) โดยเฉพาะแบบ JSON บรรทัดเดียวของ iPergo และไฟล์เฉพาะโครงสร้างที่พังง่ายที่สุด · เพิ่ม **§ 10 วิธีย้อนกลับ 3 ระดับ** รวมถึงการ Rollback บน Cloudflare ที่ไม่ต้องแตะโค้ด · เพิ่ม **§ 11.2 รายการราคาที่เขียนซ้ำหลายที่** (พ่นสีฝุ่น Izip 4 จุด · รีโมท MA3 LED 2 จุด · ราคาสำรองใน `*-structure.html` และ `seatel-st1000e`) · แก้ตัวอย่างที่ผิดในคู่มือเดิม (ตัวอย่าง `sw.js` เคยเขียน `v4` ทั้งที่ของจริงเป็น v22 · ตัวเลขมอเตอร์ 4 รุ่นไม่ตรงกับ `prices.js`) · **เพิ่มไฟล์ `deploy.bat` · `backup.bat` · `login.bat`** (พร้อมไฟล์คู่ `.ps1`) ให้ deploy และสำรองไฟล์ได้ด้วยการดับเบิลคลิก ไม่ต้องพิมพ์คำสั่งใน terminal — `deploy.bat` แสดงเลขเวอร์ชัน `sw.js` ให้ดูก่อนยืนยันด้วย กันลืม bump. **หมายเหตุทางเทคนิค:** ไฟล์ `.bat` ต้องเป็น ASCII ล้วน (ข้อความไทยใน `.bat` จะเพี้ยนเพราะ codepage ของ cmd) ข้อความไทยทั้งหมดจึงอยู่ในไฟล์ `.ps1` ที่บันทึกเป็น UTF-8 พร้อม BOM. bump `sw.js` → v23
- **1 ส.ค. 2026** — **`fabric-price-calculator.html` เพิ่มช่อง "หน้ากว้างผ้า" ที่แก้เองได้ และช่อง "การต่อผ้า" (ต่อแนวตั้ง / ต่อแนวนอน)**: เดิมหน้ากว้างล็อกตามตาราง `roll` และบังคับหารด้วย **ความกว้างงาน** อย่างเดียวเสมอ ตอนนี้เลือกได้ว่าจะให้หน้าผ้าพาดตามความกว้างหรือความยาว ซึ่ง **ราคาต่างกัน** (งาน 3.00 × 5.00 ม. บนผ้า 2.50 ม.: แนวตั้ง 25.00 ตร.ม. · แนวนอน 15.00 ตร.ม.) ค่าตั้งต้นคือ **ต่อแนวตั้ง** = ผลลัพธ์เท่าเดิมทุกใบเสนอราคาที่เคยคิดไว้. ช่องหน้ากว้างเด้งตามผ้าที่เลือก พิมพ์ทับได้ มีปุ่มรีเซ็ต และขึ้น `(กำหนดเอง)` ในข้อความส่งลูกค้า. ข้อความส่งลูกค้าเปลี่ยนบรรทัด `จำนวนท่อนที่ตัด` เป็น `การต่อผ้า: ...ท่อน · รอยต่อ N` (แปล EN แล้ว). **เพิ่มช่อง "ระยะทับกันของรอยต่อ / Seam Overlap" (ซม. ค่าตั้งต้น `0`)** — เดิมไม่ได้คิดผ้าส่วนที่ต้องวางทับกันก่อนเย็บเลย ตอนนี้ระยะทับมีผลกับ **จำนวนท่อน** ด้วย (ผ้า 2.50 ม. ทับกัน 5 ซม. → 2 ท่อนคลุมได้แค่ 4.95 ม. งานกว้าง 5.00 ม. จึงต้องใช้ 3 ท่อน ราคา 15.00 → 22.50 ตร.ม.) มี error กันกรณีใส่ระยะทับเกินหน้ากว้างผ้า และคำเตือนเมื่อมีรอยต่อแต่ยังไม่ได้เผื่อ. **เพิ่มรูป "ผังการตัดต่อผ้า"** เหนือข้อความส่งลูกค้า วาดตามสเกลจริง โชว์กรอบงานจริง (ฟ้า) · ผ้าที่จมในตะเข็บ (ส้ม) · เศษเจียนทิ้ง (ลายแดง) · เส้นรอยเย็บพร้อมเลขท่อน — รูปนี้ไม่ติดไปกับข้อความที่คัดลอก. **แก้บั๊กปัดเศษทศนิยม** ที่ทำให้งานลงตัวพอดีถูกคิดเกินไป 1 ท่อน (เช่น 4.08 ÷ 2.04 เคยได้ 3 ท่อน ตอนนี้ได้ 2 ท่อน) — บั๊กนี้จะเจอบ่อยขึ้นมากเมื่อพิมพ์หน้ากว้างเอง. bump `sw.js` → v22
- **31 ก.ค. 2026** — **เพิ่มเครื่องคำนวณราคาผ้าใบ** `fabric-price-calculator.html` สร้างจากตารางราคาขายผ้าใบ (Fabric Price, Update 15 Oct 2025): ผ้า **28 รายการ** (Solar Series 20 · SIOEN 7 · ECO-LINE 1) · **2 โหมดราคา** — ราคาเฉพาะผ้า (ต่อเมตร หรือ ต่อ ตร.ม. ตามชนิดผ้า) และ ราคาตัดเย็บ **Price A / B / C** ต่อ ตร.ม. · **คิดเศษอัตโนมัติตามหน้ากว้างผ้า** (งานเล็กกว่าหน้าผ้าคิดเต็มหน้าผ้า งานกว้างกว่าหน้าผ้าคิดเป็นท่อน) พร้อมแสดงเศษที่คิดเป็น ตร.ม. · เพิ่มด้ายเย็บ (DABOND `600` · Outdoor Pro 40 `1,350` · TENARA `6,000` · CUSBOR `4,000` บาท/หลอด) · Solar Trim `15` บาท/หลา · ค่าขนส่ง+ติดตั้งกรณีเปลี่ยนผ้าใช้โครงเดิม `300` บาท/ตร.ม. (ขั้นต่ำ `3,000`). เพิ่มหมวด **Fabric Only** ใน `index.html`. bump `sw.js` → v21
- **23 ก.ค. 2026** — **เพิ่มช่องติ๊ก 3 รายการใหม่** ในกลุ่มอุปกรณ์เสริม Somfy ของ **10 เครื่องคำนวณ**: รีโมท 6 ช่อง (`13,675`) · รีโมท 16 ช่อง (`12,432`) · สวิตซ์ Smoove 1 ช่อง RTS Origin (`2,362`) — ราคาอ่านจาก `prices.js` ทั้งหมด (คีย์ `remote6` / `remote16` / `smoove`) ป้ายราคาอัปเดตอัตโนมัติ. ไฟล์ที่เพิ่ม: `seatel-st2000`, `chill-d1`, `izip-screen`, `seatel-st1000e-vertical-awning`, `terra-ma3`, `spettmann-star` และไฟล์เฉพาะโครงสร้างทั้ง 4
- **23 ก.ค. 2026** — **`seatel-st2000.html` เพิ่มช่องติ๊กรีโมทครบชุด** (เดิมมีแต่เซ็นเซอร์ ไม่มีรีโมทเลย): รีโมท 1 · 2 · 5 · 6 · 16 ช่อง และสวิตซ์ Smoove อ่านราคาจาก `prices.js` ทั้งหมด พร้อมหมายเหตุเตือนว่ามอเตอร์บางรุ่นรวมรีโมท 1 ช่องมาแล้ว ให้ติ๊กเฉพาะรีโมทที่ต้องการเพิ่ม (กันคิดราคารีโมทซ้ำ)
- **23 ก.ค. 2026** — แก้ราคาสำรอง (`data-price`) ใน `seatel-st1000e-vertical-awning.html` ที่ยังค้างเลขเก่า (รีโมท 1/2/5 ช่อง = 3,017 / 3,862 / 6,035) ให้ตรงกับ catalog (3,108 / 3,978 / 6,216). bump `sw.js` → v19
- **23 ก.ค. 2026** — ย้าย **ราคาฝาครอบกันน้ำ (Cover Profile)** ไปไว้ในบล็อก `coverProfile` ของ `prices.js` = single source of truth: ฝาครอบอลูมิเนียม/สีเงิน MF `2,400` · ฝาครอบสีขาว WH `2,900` (**หน่วยเป็นบาทต่อความกว้าง 1 เมตร** ราคาจริง = ราคาต่อเมตร × ความกว้าง). ทั้ง **7 เครื่องคำนวณ** ที่มีฝาครอบดึงราคาจากที่นี่แล้ว (`seatel-st2000`, `seatel-st1000e-vertical-awning`, `chill-d1`, `ma4-calculator`, `spettmann-star`, `terra-ma3`, `terra-ma3-LED`) — ทั้งตัวเลขที่ใช้คำนวณและตัวเลขบนป้ายตัวเลือกอัปเดตอัตโนมัติ. เพิ่ม `<script src="prices.js">` ให้ `terra-ma3-LED.html` ซึ่งเดิมยังไม่ได้โหลด catalog. bump `sw.js` → v18
- **23 ก.ค. 2026** — แก้ **syntax error ใน `prices.js`** ที่ทำให้ไฟล์โหลดไม่ได้ทั้งไฟล์ (คีย์ `สวิตซ์ Smoove` มีเว้นวรรค ใช้เป็นชื่อคีย์ใน JavaScript ไม่ได้) → เปลี่ยนชื่อคีย์เป็น `smoove` และย้ายคำอธิบายภาษาไทยไปไว้ในคอมเมนต์. **ข้อควรระวัง:** ชื่อคีย์ต้องเป็นภาษาอังกฤษติดกัน ห้ามมีเว้นวรรค มิฉะนั้นเครื่องคำนวณทุกตัวที่โหลด `prices.js` จะคำนวณราคาอุปกรณ์เสริมไม่ได้ทั้งหมด
- **23 ก.ค. 2026** — **ปรับราคารีโมท Somfy:** รีโมท 1 ช่อง `3,017 → 3,108` · 2 ช่อง `3,862 → 3,978` · 5 ช่อง `6,035 → 6,216`. **เพิ่มรายการใหม่ใน `prices.js`:** รีโมท 6 ช่อง (`remote6` = 13,675) · รีโมท 16 ช่อง (`remote16` = 12,432) · สวิตซ์ Smoove 1 ช่อง RTS Origin (`smoove` = 2,362) — รายการใหม่ 3 ตัวนี้ยังไม่ได้ผูกกับช่องติ๊กในเครื่องคำนวณตัวไหน (เก็บไว้ใน catalog รอใช้งาน)
- **23 ก.ค. 2026** — **ปรับราคามอเตอร์ในบล็อก `motors`:** Somfy Altus 40/17 `26,500 → 26,800` · Altus 85/17 `36,500 → 36,800` · Maestria 15/17 `24,500 → 25,000` · Maestria 35/17 `30,500 → 31,000`. deploy ขึ้น Cloudflare แล้ว
- **22 ก.ค. 2026** — ลบหัวข้อซ้ำซ้อน **"Customer copy text / ข้อความสำหรับส่งลูกค้า"** ที่อยู่ใต้หัวข้อ "Quotation Preview / ข้อความส่งลูกค้า" ออกจากทุกเครื่องคำนวณที่มี (15 ไฟล์ รวม zipguide-expert/ultra, izip-screen, terra-fs ทุกตัว ฯลฯ) — เป็นข้อความบอกซ้ำจึงเอาออก. ป้ายข้อความในเนื้อข้อความที่คัดลอกส่งลูกค้า (customer-copy-label) ยังคงไว้เหมือนเดิม. bump `sw.js` → v17 และ deploy ขึ้น Cloudflare แล้ว
- **22 ก.ค. 2026** — ขยายบล็อก `motors` ใน `prices.js` ให้ครอบคลุมมอเตอร์ Somfy ทุกรุ่นที่ใช้ในเครื่องคำนวณแบบเลือกมอเตอร์ได้ และเชื่อมไฟล์ `chill-d1`, `seatel-st2000`, `seatel-st1000e-vertical-awning`, `izip-screen` ให้ดึงราคามอเตอร์จาก `prices.js` (ผ่าน `data-price-key` ในแต่ละ `<option>`) = แก้ราคามอเตอร์ที่ `prices.js` ที่เดียว เปลี่ยนพร้อมกันทุกไฟล์. **ปรับราคา:** Somfy Altus 85/17 `32,800 → 36,500` · Altus 40/17 `25,800 → 26,500` (seatel-st2000) · Altus 30/17 `24,500 → 24,800` (seatel-st1000e). ไฟล์ที่ราคามอเตอร์รวมในตารางแล้ว (`ma4-calculator`, `terra-ma3`, `spettmann-star`) ไม่ถูกแก้ (แต่ตัวเลือก Somfy 85/17 ที่อ่านจาก catalog จะเป็น 36,500 ตามด้วย)
- **22 ก.ค. 2026** — ย้าย **ราคาตัวมอเตอร์** (Somfy Altus 50/12 · 85/17 · Strong Up 50/12 · 50/12+มือหมุน · 80/15) ไปไว้ในบล็อก `motors` ของ `prices.js` = single source of truth; ไฟล์เฉพาะโครงสร้างทั้ง 4 อ่านราคาจาก catalog แทนการฝังในโค้ด และเพิ่มตัวเลือก **Strong Up 50/12 + มือหมุน (11,900)** ป้ายราคาในช่องเลือกมอเตอร์อัปเดตอัตโนมัติจาก catalog
- **22 ก.ค. 2026** — เพิ่มการเลือก **มอเตอร์ + อุปกรณ์เสริม** ในไฟล์เฉพาะโครงสร้างทั้ง 4 ไฟล์ (`ma4-calculator-structure.html`, `terra-ma3-structure.html`, `seatel-st2000-structure.html`, `spettmann-star-structure.html`): มอเตอร์ Somfy Altus 50/12 · 85/17 และ Strong Up 50/12 · 80/15 พร้อมอุปกรณ์เสริม Somfy/Strong Up ที่อ่านราคาจาก `prices.js` — ไฟล์เหล่านี้เริ่มโหลด `prices.js` ด้วย

---

*ปรับปรุงล่าสุด: 3 ส.ค. 2026 · โปรเจกต์ Sangthong Awning Calculator · เว็บจริง: awning-calculator.pages.dev (Cloudflare Pages)*
