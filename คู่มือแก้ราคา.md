# คู่มือแก้ไขราคาเครื่องคำนวณกันสาด (สำหรับเจ้าหน้าที่)

คู่มือนี้สำหรับ **เจ้าหน้าที่ธุรการ** ที่ต้องอัปเดตราคาในเครื่องคำนวณ (ไฟล์ `.html`) เมื่อมีราคาชุดใหม่จากโรงงาน
ไม่ต้องเขียนโปรแกรมเป็นก็ทำได้ ขอแค่ทำตามขั้นตอนอย่างระมัดระวัง

> **สรุปสั้น ๆ ก่อนเริ่ม**
> 1. หาไฟล์ที่ถูกต้อง → 2. แก้เฉพาะ "ตัวเลข" → 3. เพิ่มเลขเวอร์ชันใน `sw.js` → 4. ทดสอบ → 5. ส่งขึ้น GitHub

---

## ⚠️ กฎเหล็ก 6 ข้อ (อ่านก่อนแก้ทุกครั้ง)

1. **แก้ได้เฉพาะตัวเลข** เท่านั้น อย่าลบเครื่องหมาย `,` `[` `]` `{` `}` `"` `:` หรือ `//`
2. **ห้ามใส่ลูกน้ำในตัวเลข** ในโค้ด — พิมพ์ `58070` ไม่ใช่ `58,070`
3. **แก้ทีละไฟล์ ทีละรุ่น** อย่าแก้หลายอย่างพร้อมกัน จะได้ตรวจง่ายถ้าผิด
4. **คัดลอกไฟล์เก็บสำรองก่อนแก้เสมอ** (Copy วางไว้อีกที่ก่อน)
5. **ทุกครั้งที่แก้ ต้องเพิ่มเลขเวอร์ชันใน `sw.js`** ไม่งั้นลูกค้าจะยังเห็นราคาเก่า (ดูหัวข้อที่ 5)
6. **ทดสอบในเบราว์เซอร์ก่อน deploy ขึ้นเว็บจริงเสมอ** อย่าเผยแพร่ทั้งที่ยังไม่ได้ลอง

---

## 1. ทำความรู้จักไฟล์ต่าง ๆ

| ไฟล์ | คืออะไร | เปิดด้วย |
|---|---|---|
| `terra-ma3.html`, `seatel-st2000.html`, `chill-d1.html` ฯลฯ | เครื่องคำนวณ 1 ไฟล์ = 1 รุ่นสินค้า | เบราว์เซอร์ (ดู) / Notepad หรือ VS Code (แก้) |
| `*-structure.html` (เช่น `ma4-calculator-structure.html`) | เครื่องคำนวณ **เฉพาะราคาโครงสร้าง** (ไม่รวมผ้า) + เลือกมอเตอร์/อุปกรณ์เสริมได้ | เบราว์เซอร์ / VS Code |
| `prices.js` | คลังราคา **อุปกรณ์เสริม** ที่ใช้ร่วมกันหลายรุ่น (รีโมท เซ็นเซอร์ ฯลฯ) | Notepad / VS Code |
| `sw.js` | ตัวจัดการ "เวอร์ชัน" ของเว็บ ต้องเพิ่มเลขทุกครั้งที่แก้ | Notepad / VS Code |
| `index.html` | หน้ารวมลิงก์เครื่องคำนวณทุกตัว | — |

> **แนะนำ:** ติดตั้งโปรแกรม **VS Code** (ฟรี) เพื่อแก้ไฟล์ จะเห็นสีของโค้ดชัดเจน ลดโอกาสพิมพ์ผิดกว่าใช้ Notepad มาก

---

## 2. ราคามี 3 แบบ อยู่คนละที่

ก่อนแก้ ต้องรู้ว่าราคาที่จะแก้เป็น **แบบไหน** เพราะอยู่คนละที่

### แบบ A — ราคาตามขนาด (อยู่ในไฟล์ของรุ่นนั้น ๆ)
ราคาที่เปลี่ยนตาม **ความกว้าง × ระยะยื่น** เช่น ตารางราคากันสาด Terra MA3
→ อยู่ใน **ไฟล์ HTML ของรุ่นนั้น** (เช่น `terra-ma3.html`)

### แบบ B — ราคาอุปกรณ์เสริม (อยู่ใน `prices.js`)
รีโมท 1 ช่อง, รีโมท 15 ช่อง, เซ็นเซอร์ลม-แดด, อุปกรณ์ Somfy ฯลฯ
→ อยู่ใน **`prices.js` ที่เดียว** แก้ที่นี่ครั้งเดียว ทุกรุ่นที่ใช้ร่วมกันจะเปลี่ยนตามอัตโนมัติ

### แบบ C — ราคาตัวมอเตอร์ (ส่วนใหญ่อยู่ใน `prices.js` แล้ว)
ราคามอเตอร์ Somfy / Strong Up ของเครื่องคำนวณที่ให้ **เลือกมอเตอร์ได้** อ่านจากบล็อก `motors` ใน `prices.js` ที่เดียว (ดูรายละเอียดในกรณีพิเศษด้านล่าง) — ไม่ต้องแก้ในไฟล์ HTML ของแต่ละรุ่น
ยกเว้นบางรุ่นที่ราคามอเตอร์ถูก **รวมในตารางราคาตามขนาด** ไปแล้ว (แบบ A) เช่น `ma4-calculator.html`, `terra-ma3.html`, `spettmann-star.html` — พวกนี้แก้ที่ตารางในไฟล์รุ่นนั้น

### แบบ D — ราคาฝาครอบกันน้ำ / Cover Profile (อยู่ใน `prices.js`)
คิดเป็น **บาทต่อความกว้าง 1 เมตร** (ราคาจริง = ราคาต่อเมตร × ความกว้างเป็นเมตร) เก็บอยู่ในบล็อก `coverProfile` ของ `prices.js` ที่เดียว ใช้ร่วมกัน **7 เครื่องคำนวณ**: `seatel-st2000`, `seatel-st1000e-vertical-awning`, `chill-d1`, `ma4-calculator`, `spettmann-star`, `terra-ma3`, `terra-ma3-LED`

**วิธีแก้:** เปิด `prices.js` → หาบล็อก `coverProfile:` → แก้เฉพาะตัวเลข (ห้ามใส่ลูกน้ำ)

```js
coverProfile: {
  aluminum: 2400,   // ฝาครอบอลูมิเนียม MF (สีเงิน) — บาท/เมตร
  white:    2900    // ฝาครอบสีขาว WH — บาท/เมตร
}
```

> แก้ครั้งเดียว เปลี่ยนพร้อมกันทั้ง 7 ไฟล์ ทั้ง **ตัวเลขที่ใช้คำนวณ** และ **ตัวเลขราคาบนป้ายตัวเลือก** (เช่น `ฝาปิดอลูมิเนียม (2,400 บาท × ความกว้าง)`) — ไม่ต้องไปแก้ HTML แต่ละไฟล์
> ถ้าจะ **เพิ่มสีฝาครอบใหม่** ต้องเพิ่มทั้งคีย์ใน `coverProfile` และ `<option>` ในไฟล์ที่ต้องการ พร้อมใส่ `data-price-key="coverProfile.<คีย์ใหม่>"`

### ⭐ กรณีพิเศษ — ราคาผ้าใบ (`fabric-price-calculator.html`)
ราคาขายผ้าใบทั้งตาราง (Fabric Price) อยู่ใน **ไฟล์เดียว** คือ `fabric-price-calculator.html`
เปิดไฟล์แล้วหาบล็อก `const FABRIC_PRICES = [` (อยู่ต้น ๆ ของ `<script>`) แต่ละบรรทัดคือผ้า 1 ชนิด

```js
{ key:'solar_max', group:'Solar Series', label:'Solar Max - ผ้าใบ Coated PVC Sioen B8000 630 g',
  roll:2.50, len:60, unit:'sqm', price:350, a:550, b:650, c:900 },
```

| ช่อง | ความหมาย |
|---|---|
| `roll` | **หน้ากว้างผ้ามาตรฐาน** (เมตร) — เป็นแค่ **ค่าตั้งต้น** ที่เด้งไปใส่ในช่อง "หน้ากว้างผ้า" ตอนเลือกผ้า ผู้ใช้พิมพ์ทับได้ ถ้าใส่ผิดพื้นที่จะคิดผิดทั้งหมด |
| `len` | ความยาวต่อม้วน (เมตร) — แสดงเป็นข้อมูลเฉย ๆ ไม่ได้ใช้คำนวณ |
| `unit` | หน่วยของ `price` เท่านั้น: `'m'` = ราคาต่อเมตร · `'sqm'` = ราคาต่อตารางเมตร |
| `price` | **ราคาเฉพาะผ้า** (ยังไม่ตัดเย็บ) ตามหน่วย `unit` |
| `a` `b` `c` | **ราคาตัดเย็บ** Price A / B / C หน่วยเป็น **บาท/ตร.ม. เสมอ** |

> ⚠️ **ห้ามสลับ `unit:'m'` กับ `unit:'sqm'`** เพราะคิดเงินคนละแบบ (ต่อเมตร = ยาว × ราคา / ต่อ ตร.ม. = พื้นที่ × ราคา) ให้ยึดตามคอลัมน์ "ราคาเฉพาะผ้า/หน่วย" ในตารางราคาต้นฉบับ
> ราคาด้ายเย็บอยู่ในบล็อก `THREADS` ถัดลงมา · Solar Trim อยู่ที่ `TRIM_PRICE_PER_YARD` (บาท/หลา) · ค่าขนส่ง+ติดตั้งอยู่ที่ `INSTALL_RATE_PER_SQM` (300 บาท/ตร.ม.) และ `INSTALL_MINIMUM` (ขั้นต่ำ 3,000 บาท)
> ไฟล์นี้ **ไม่ได้ใช้ `prices.js`** — แก้ราคาผ้าที่ไฟล์นี้ที่เดียว แล้ว bump `sw.js` ตามปกติ

**หน้ากว้างผ้า + การต่อผ้า (มีผลกับพื้นที่ที่คิดเงินโดยตรง)**

ช่อง **"หน้ากว้างผ้า / Fabric Width"** ตั้งต้นจาก `roll` ของผ้าที่เลือก แต่ **พิมพ์ทับได้** ถ้าผ้าที่มีอยู่จริงหน้ากว้างไม่เท่าตาราง (เช่น ของค้างสต๊อก หรือซัพพลายเออร์ส่งมาไม่เท่าเดิม) เมื่อกำหนดเองจะมีคำเตือนสีแดงใต้ช่อง พร้อมปุ่ม **รีเซ็ต** กลับค่ามาตรฐาน และในข้อความส่งลูกค้าจะต่อท้ายว่า `(กำหนดเอง)` — **เปลี่ยนชนิดผ้าเมื่อไหร่ ค่าจะถูกดึงกลับเป็นค่ามาตรฐานของผ้าใหม่เสมอ**

ช่อง **"การต่อผ้า / Joining Pattern"** เลือกได้ว่าจะเอา **ด้านไหนของงาน** ไปหารด้วยหน้ากว้างผ้า:

| ตัวเลือก | หน้าผ้าพาดตาม | จำนวนท่อน | ความยาวผ้าที่ดึงจากม้วน |
|---|---|---|---|
| **ต่อแนวตั้ง** (ค่าตั้งต้น) | ความกว้างงาน | ความกว้าง ÷ หน้ากว้างผ้า | จำนวนท่อน × ความยาว |
| **ต่อแนวนอน** | ความยาวงาน | ความยาว ÷ หน้ากว้างผ้า | จำนวนท่อน × ความกว้าง |

> 💡 **สองแบบนี้ราคาไม่เท่ากัน** ลองสลับดูก่อนเสนอราคา ตัวอย่างงาน **กว้าง 3.00 × ยาว 5.00 ม. บนผ้าหน้ากว้าง 2.50 ม.**
> · ต่อแนวตั้ง → 2 ท่อน คิด **25.00 ตร.ม.**
> · ต่อแนวนอน → 2 ท่อน คิด **15.00 ตร.ม.** ← ประหยัดกว่า 10 ตร.ม.
> เครื่องคำนวณ **ไม่ได้เลือกแบบที่ถูกที่สุดให้อัตโนมัติ** ต้องสลับดูเอง (ค่าตั้งต้นคือต่อแนวตั้ง เหมือนที่เคยคิดมาก่อนหน้านี้)

**ระยะทับกันของรอยต่อ / Seam Overlap (ซม.)**

เวลาต่อผ้าจริงต้องเอาผ้าสองท่อน **วางทับกัน** ก่อนเย็บ ผ้าส่วนที่ทับกันนี้จ่ายเงินสองรอบแต่ได้หน้ากว้างรอบเดียว ช่องนี้กรอกเป็น **เซนติเมตร** (ค่าตั้งต้น `0` = ไม่คิดเผื่อ เท่ากับที่เคยคิดมาตลอด)

> ⚠️ **ระยะทับมีผลกับ "จำนวนท่อน" ไม่ใช่แค่พื้นที่** เพราะท่อนที่ 2 เป็นต้นไปคลุมหน้ากว้างได้แค่ (หน้ากว้าง − ระยะทับ)
> ตัวอย่าง ผ้าหน้ากว้าง 2.50 ม. ทับกัน 5 ซม. → 2 ท่อนคลุมได้แค่ **4.95 ม.** ไม่ใช่ 5.00 ม.
> ดังนั้นงาน **กว้าง 5.00 ม. ต้องใช้ 3 ท่อน** (ไม่ใช่ 2) ราคาเลยกระโดดจาก 15.00 → 22.50 ตร.ม.
> · สูตร: `จำนวนท่อน = ปัดขึ้น( (ด้านที่ต่อ − ระยะทับ) ÷ (หน้ากว้างผ้า − ระยะทับ) )`
> · ถ้าใส่ระยะทับ **มากกว่าหรือเท่ากับหน้ากว้างผ้า** จะขึ้น error เพราะต่อยังไงก็ไม่มีวันคลุมงานได้
> · ถ้างานใช้ท่อนเดียว (ไม่มีรอยต่อ) ระยะทับจะไม่ถูกนำมาคิด
> 💡 ถ้ามีรอยต่อแต่ยังใส่ `0` อยู่ จะมีคำเตือนสีแดงใต้ช่องว่า "ผ้าที่สั่งอาจไม่พอเย็บ"
> ถ้าร้านมีมาตรฐานทับกันประจำ (เช่น 5 ซม.) เปลี่ยนค่าตั้งต้นได้ที่ `<input id="seamOverlap" ... value="0"/>` ใน `fabric-price-calculator.html`

**ผังการตัดต่อผ้า (รูปในกล่องผลลัพธ์)**

เหนือข้อความส่งลูกค้าจะมีรูป **ผังการตัดต่อผ้า** วาดตามสเกลจริง บอกว่า
กรอบ**ฟ้า** = ขนาดงานจริง · แถบ**ส้ม** = ผ้าที่จมอยู่ในตะเข็บ (ส่วนที่ทับกัน) · พื้นที่**ลายแดง** = เศษที่ต้องเจียนทิ้ง · เส้น**ประน้ำเงิน** = รอยเย็บ (มีเลขกำกับท่อน)
ใต้รูปมีตัวเลขแยกให้ว่าเศษที่จ่ายไปหายไปไหนบ้าง — **จมในตะเข็บ** กี่ ตร.ม. และ **เจียนทิ้ง** กี่ ตร.ม.
สลับ **ต่อแนวตั้ง/ต่อแนวนอน** แล้วดูรูปเทียบกันได้ทันทีว่าแบบไหนเศษน้อยกว่า และรูปเปลี่ยนภาษาตามปุ่ม TH/EN ด้วย

> รูปนี้ **ไม่ติดไปกับข้อความที่กดคัดลอกส่งลูกค้า** (ข้อความยังเป็นตัวหนังสือล้วนเหมือนเดิม) เป็นภาพช่วยให้ช่างและคนเสนอราคาเห็นภาพเฉย ๆ
> ⚠️ ยังไม่เตือนกรณีผ้าที่ต้องใช้ยาวเกิน 1 ม้วน (`len`)

### ⭐ กรณีพิเศษ — ไฟล์ "เฉพาะโครงสร้าง" (`*-structure.html`)
ไฟล์กลุ่มนี้ (MA4, Terra MA3, Seatel ST2000, Spettmann Star) คิด **ราคาโครงสร้างอย่างเดียว** และให้ **เลือกมอเตอร์ + อุปกรณ์เสริม** เพิ่มได้ ทั้ง **ราคาตัวมอเตอร์** และ **ราคาอุปกรณ์เสริม** อ่านจาก **`prices.js`** ที่เดียว (แก้ครั้งเดียว เปลี่ยนพร้อมกันทั้ง 4 ไฟล์):

- **ราคาตัวมอเตอร์** (Somfy Altus 50/12, 85/17 · Strong Up 50/12, 50/12+มือหมุน, 80/15) → บล็อก `motors` ใน `prices.js`
- **ราคาอุปกรณ์เสริมมอเตอร์** (รีโมท เซ็นเซอร์ ฯลฯ) → บล็อก `somfy` / `strongUp` ใน `prices.js` เหมือนแบบ B

**วิธีแก้ราคามอเตอร์:** เปิด `prices.js` → หาบล็อก `motors:` → แก้เฉพาะตัวเลข (ห้ามใส่ลูกน้ำ)

```js
motors: {
  // Strong Up (ราคาเท่ากันทุกไฟล์)
  strongUp50_12:           9900,   // Strong Up 50/12
  strongUp50_12_crank:     11900,  // Strong Up 50/12 + มือหมุน
  strongUp80_15:           15900,  // Strong Up 80/15
  // Somfy Altus
  somfyAltus50_12:         26800,  // Somfy Altus 50/12
  somfyAltus30_17:         24800,  // Somfy Altus 30/17
  somfyAltus40_17:         26500,  // Somfy Altus 40/17
  somfyAltus85_17:         36500,  // Somfy Altus 85/17
  // Somfy Maestria
  somfyMaestria15_17:      24500,  // Somfy Maestria 15/17
  somfyMaestria35_17:      30500,  // Somfy Maestria 35/17
  // Somfy MAESTRIA+50 RTS
  somfyMaestria50RTS15_17: 25000,  // MAESTRIA+50 RTS 15/17
  somfyMaestria50RTS35_17: 31000,  // MAESTRIA+50 RTS 35/17
  // Somfy Altus 50 RTS (เฉพาะ izip-screen)
  somfyAltus50RTS10_17:    21800,
  somfyAltus50RTS20_17:    23800,
  somfyAltus50RTS30_17:    24800,
  somfyAltus50RTS40_17:    25800
}
```

> ป้ายราคาในหน้าจอ (ช่องเลือกมอเตอร์) จะอัปเดตตามอัตโนมัติ ไม่ต้องไปแก้ HTML
> **หมายเหตุ:** ตอนนี้เครื่องคำนวณ `chill-d1`, `seatel-st2000`, `seatel-st1000e-vertical-awning`, `izip-screen` ดึงราคามอเตอร์จากบล็อก `motors` ใน `prices.js` แล้วเช่นกัน (ผ่าน `data-price-key` ในแต่ละ `<option>`) — แก้ราคาที่ `prices.js` ที่เดียว เปลี่ยนพร้อมกันทุกไฟล์ ยกเว้นไฟล์ที่ราคามอเตอร์ถูกรวมในตารางแล้ว (`ma4-calculator.html`, `terra-ma3.html`, `spettmann-star.html`)

---

## 3. วิธีแก้ "ราคาตามขนาด" (แบบ A)

**ตัวอย่าง:** ราคากันสาด Terra MA3 ผ้า Sunbrella ระบบมือหมุน ปรับขึ้น

### 3.1 เปิดไฟล์
เปิด `terra-ma3.html` ด้วย VS Code

### 3.2 ค้นหาตารางที่ถูกต้อง
กด `Ctrl + F` แล้วพิมพ์ชื่อตาราง ตารางราคาจะตั้งชื่อแบบนี้:

- `PRICE_MANUAL_...` = ระบบ **มือหมุน**
- `PRICE_MOTOR_...` = ระบบ **มอเตอร์**
- ท้ายชื่อคือชนิดผ้า เช่น `_ACRYLIC` `_SOLAR` `_SUNBRELLA` `_NOVA`

เช่น อยากแก้ผ้า Sunbrella มือหมุน → ค้นหา `PRICE_MANUAL_SUNBRELLA`

### 3.3 เข้าใจโครงสร้างตาราง
ตารางหน้าตาแบบนี้ (ตัวเลขในวงเล็บ `//` คือคำอธิบาย ไม่ต้องแก้):

```js
const PRICE_MANUAL_SUNBRELLA = {
  "1.50": [        // ← ระยะยื่น 1.50 เมตร
    25350, // 2.00–2.50   ← ความกว้าง (ช่องแรก)
    27780, // 2.51–3.00
    30270, // 3.01–3.50
    ...
  ],
  "2.00": [        // ← ระยะยื่น 2.00 เมตร
    null,  // 2.00–2.50 ไม่มี   ← null = ช่องที่ขายไม่ได้ (ในตารางเป็น x)
    31130, // 2.51–3.00
    ...
```

**อ่านตารางยังไง:**
- บรรทัด `"1.50":` `"2.00":` ฯลฯ = **ระยะยื่น (เมตร)**
- ตัวเลขแต่ละบรรทัดในวงเล็บ `[ ]` = **ช่วงความกว้าง** เรียงจากแคบ (2.00–2.50) ไปกว้าง (7.51–8.00)
- คำอธิบายหลัง `//` บอกว่าเลขนั้นคือช่วงความกว้างไหน

### 3.4 แก้ตัวเลข
หาช่องที่ต้องการ แล้วพิมพ์ทับเฉพาะตัวเลข **อย่าลืมเครื่องหมาย `,` ท้ายบรรทัดไว้เหมือนเดิม**

```js
    25350, // 2.00–2.50      ← เดิม
    26100, // 2.00–2.50      ← แก้เป็นราคาใหม่ (คง , ไว้)
```

- ในตาราง PDF ช่องไหนเป็น **x** → ในโค้ดใช้คำว่า **`null`** (อย่าใส่ 0)
- **ห้ามใส่ลูกน้ำในตัวเลข** → `26100` ✅ ไม่ใช่ `26,100` ❌

### 3.5 บันทึกไฟล์
กด `Ctrl + S`

---

## 4. วิธีแก้ "ราคาอุปกรณ์เสริม" (แบบ B — `prices.js`)

**ตัวอย่าง:** รีโมท 1 ช่อง ขึ้นจาก 950 เป็น 990 บาท

### 4.1 เปิด `prices.js`

### 4.2 หาบรรทัดที่ต้องแก้
ราคาอยู่เป็นรายการชัดเจน เช่น:

```js
  strongUp: {
    remote1:      950,   // Transmitter 1 Channel / รีโมท 1 ช่อง
    remote15:    1800,   // Transmitter 15 Channel / รีโมท 15 ช่อง
    windSun:     6600,   // Wind-Sun Sensor / เซ็นเซอร์ลม-แดด
    ...
```

### 4.3 แก้เฉพาะตัวเลข (คง `,` ไว้)

```js
    remote1:      990,   // Transmitter 1 Channel / รีโมท 1 ช่อง
```

> **ข้อดี:** แก้ `prices.js` ครั้งเดียว ทั้ง "ราคาที่คำนวณ" และ "ป้ายราคาที่แสดงในหน้าจอ" จะเปลี่ยนตามอัตโนมัติทุกรุ่นที่ใช้ร่วมกัน ไม่ต้องไล่แก้ทีละไฟล์

### 4.4 บันทึกไฟล์ `Ctrl + S`

---

## 5. ⭐ ขั้นตอนสำคัญที่สุด — เพิ่มเลขเวอร์ชันใน `sw.js`

**ถ้าข้ามขั้นตอนนี้ ลูกค้าจะยังเห็นราคาเก่า!** เพราะเว็บถูกเก็บไว้ในเครื่อง (offline)

### 5.1 เปิด `sw.js`
### 5.2 หาบรรทัดนี้ (อยู่ต้น ๆ ไฟล์)

```js
const CACHE_NAME = 'sangthong-awning-v4';
```

### 5.3 เพิ่มเลขขึ้น 1

```js
const CACHE_NAME = 'sangthong-awning-v5';   // v4 → v5
```

ครั้งต่อไปที่แก้อะไรก็ตาม ให้เพิ่มเป็น `v6`, `v7`, ... ไปเรื่อย ๆ

### 5.4 บันทึกไฟล์ `Ctrl + S`

> **จำง่าย ๆ:** แก้ราคาเมื่อไหร่ → เพิ่มเลข v ใน `sw.js` เสมอ

---

## 6. ทดสอบก่อนเผยแพร่ (ก่อน deploy ขึ้นเว็บจริง)

1. เปิดไฟล์เครื่องคำนวณที่แก้ (เช่น `terra-ma3.html`) ด้วยเบราว์เซอร์ (ดับเบิลคลิก)
2. ลองเลือกขนาด/ผ้า/อุปกรณ์ ที่ตรงกับราคาที่เพิ่งแก้
3. เช็กว่า **ราคาที่แสดงตรงกับราคาใหม่** ที่ตั้งใจไว้
4. เช็กว่าไม่มีคำว่า `NaN`, `undefined`, หรือช่องราคาว่างเปล่า
5. ถ้าเบราว์เซอร์ยังโชว์ราคาเก่า ให้กด `Ctrl + Shift + R` (โหลดใหม่แบบล้างแคช)

> ถ้าหน้าจอเพี้ยน/ราคาไม่ขึ้นเลย แปลว่าอาจพิมพ์ผิด (ลบ `,` หรือ `]` ไป) → ดูหัวข้อที่ 8

---

## 7. เผยแพร่ขึ้นเว็บจริง (Cloudflare Pages)

> เว็บใช้งานจริงอยู่ที่ **https://awning-calculator.pages.dev** โฮสต์บน **Cloudflare Pages**
> ⚠️ การแก้ไฟล์ในเครื่องหรือบน GitHub เฉย ๆ **จะไม่อัปเดตเว็บจริง** — ต้อง "deploy" ขึ้น Cloudflare ตามขั้นตอนนี้

### 7.1 เปิด Terminal ในโฟลเดอร์โปรเจกต์
- ใน **VS Code**: เมนู **Terminal → New Terminal**
- (หรือเปิด **PowerShell** แล้ว `cd` เข้าโฟลเดอร์โปรเจกต์)

### 7.2 สั่ง deploy ด้วยคำสั่งเดียว
คัดลอกคำสั่งนี้ไปวางแล้วกด Enter:

```bash
npx wrangler pages deploy . --project-name awning-calculator --branch main --commit-dirty=true
```

- รอสักครู่ จะขึ้นข้อความ **✨ Deployment complete!** พร้อมลิงก์
- เว็บจริง **https://awning-calculator.pages.dev** จะอัปเดตภายในไม่กี่วินาที

### 7.3 ครั้งแรกสุดต้อง login ก่อน (ทำครั้งเดียว)
ถ้าขึ้นว่ายังไม่ได้เข้าสู่ระบบ ให้พิมพ์:
```bash
npx wrangler login
```
เบราว์เซอร์จะเปิดให้กด **Allow** (บัญชี Cloudflare: pteerapr@gmail.com) แล้วค่อยสั่ง deploy (7.2) ซ้ำ

### 7.4 เห็นราคาเก่าอยู่? → ล้างแคช
เว็บนี้เก็บข้อมูลไว้ในเครื่อง (PWA) หลัง deploy ให้กด **`Ctrl + Shift + R`**
ถ้ายังไม่หาย: กด `F12` → แท็บ **Application** → **Storage** → **Clear site data** → รีโหลด

### 7.5 (ทางเลือก) เก็บสำเนาโค้ดไว้บน GitHub
ไม่บังคับ แต่แนะนำให้เก็บประวัติโค้ดไว้เผื่อย้อนกลับ — ใช้ **GitHub Desktop**:
พิมพ์คำอธิบายในช่อง **Summary** → **Commit to main** → **Push origin**
> GitHub เก็บแค่ "ประวัติโค้ด" — หน้าเว็บที่ลูกค้าเห็นจริงอัปเดตจากขั้นตอน deploy Cloudflare (7.2) เท่านั้น

---

## 8. แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุที่พบบ่อย | วิธีแก้ |
|---|---|---|
| ราคาไม่ขึ้น / หน้าเพี้ยน | ลบเครื่องหมาย `,` `]` `}` หรือ `"` ไปโดยไม่ตั้งใจ | เอาไฟล์สำรองที่ copy ไว้มาเทียบ แล้วใส่กลับ |
| ราคาเพี้ยนมาก (เช่น เป็นหลักสิบ) | ใส่ลูกน้ำในตัวเลข เช่น `26,100` | ลบลูกน้ำออก เหลือ `26100` |
| แก้แล้วแต่ยังเห็นราคาเก่า | ลืมเพิ่มเลข v ใน `sw.js` | เพิ่มเลข v (หัวข้อ 5) แล้ว deploy ใหม่ (หัวข้อ 7) |
| เบราว์เซอร์ตัวเองยังเห็นเก่า | แคชในเครื่อง | กด `Ctrl + Shift + R` |
| ช่องที่ควร "ไม่มี" กลับมีราคา 0 | ใส่ `0` แทน `null` | เปลี่ยนเป็น `null` |

> **ถ้าแก้แล้วพัง แก้กลับไม่ได้:** อย่าเพิ่ง deploy ขึ้นเว็บจริง ให้เอาไฟล์สำรองที่ copy ไว้ก่อนแก้มาวางทับ แล้วเริ่มใหม่

---

## 9. เช็กลิสต์ก่อนส่งงานทุกครั้ง ✅

- [ ] แก้ตัวเลขในไฟล์ที่ถูกต้องแล้ว
- [ ] ตัวเลขไม่มีลูกน้ำ (`26100` ไม่ใช่ `26,100`)
- [ ] เครื่องหมาย `,` `[ ]` `{ }` `"` ครบเหมือนเดิม
- [ ] ช่องที่ขายไม่ได้ใช้ `null`
- [ ] **เพิ่มเลขเวอร์ชันใน `sw.js` แล้ว**
- [ ] เปิดทดสอบในเบราว์เซอร์ ราคาถูกต้อง ไม่มี NaN/undefined
- [ ] **deploy ขึ้น Cloudflare เรียบร้อย** (คำสั่งในหัวข้อ 7) และเปิดเว็บจริงเช็กแล้ว

---

## 10. บันทึกการเปลี่ยนแปลง (Changelog)

> ทุกครั้งที่มีการแก้ราคา หรือเพิ่ม/เปลี่ยนฟีเจอร์ ให้บันทึกสั้น ๆ ไว้ที่นี่ (วันที่ · สิ่งที่เปลี่ยน · ไฟล์ที่เกี่ยวข้อง) จะได้ย้อนดูได้ว่าใครแก้อะไรเมื่อไหร่

- **1 ส.ค. 2026** — **`fabric-price-calculator.html` เพิ่มช่อง "หน้ากว้างผ้า" ที่แก้เองได้ และช่อง "การต่อผ้า" (ต่อแนวตั้ง / ต่อแนวนอน)**: เดิมหน้ากว้างล็อกตามตาราง `roll` และบังคับหารด้วย **ความกว้างงาน** อย่างเดียวเสมอ ตอนนี้เลือกได้ว่าจะให้หน้าผ้าพาดตามความกว้างหรือความยาว ซึ่ง **ราคาต่างกัน** (งาน 3.00 × 5.00 ม. บนผ้า 2.50 ม.: แนวตั้ง 25.00 ตร.ม. · แนวนอน 15.00 ตร.ม.) ค่าตั้งต้นคือ **ต่อแนวตั้ง** = ผลลัพธ์เท่าเดิมทุกใบเสนอราคาที่เคยคิดไว้. ช่องหน้ากว้างเด้งตามผ้าที่เลือก พิมพ์ทับได้ มีปุ่มรีเซ็ต และขึ้น `(กำหนดเอง)` ในข้อความส่งลูกค้า. ข้อความส่งลูกค้าเปลี่ยนบรรทัด `จำนวนท่อนที่ตัด` เป็น `การต่อผ้า: ...ท่อน · รอยต่อ N` (แปล EN แล้ว). **เพิ่มช่อง "ระยะทับกันของรอยต่อ / Seam Overlap" (ซม. ค่าตั้งต้น `0`)** — เดิมไม่ได้คิดผ้าส่วนที่ต้องวางทับกันก่อนเย็บเลย ตอนนี้ระยะทับมีผลกับ **จำนวนท่อน** ด้วย (ผ้า 2.50 ม. ทับกัน 5 ซม. → 2 ท่อนคลุมได้แค่ 4.95 ม. งานกว้าง 5.00 ม. จึงต้องใช้ 3 ท่อน ราคา 15.00 → 22.50 ตร.ม.) มี error กันกรณีใส่ระยะทับเกินหน้ากว้างผ้า และคำเตือนเมื่อมีรอยต่อแต่ยังไม่ได้เผื่อ. **เพิ่มรูป "ผังการตัดต่อผ้า"** เหนือข้อความส่งลูกค้า วาดตามสเกลจริง โชว์กรอบงานจริง (ฟ้า) · ผ้าที่จมในตะเข็บ (ส้ม) · เศษเจียนทิ้ง (ลายแดง) · เส้นรอยเย็บพร้อมเลขท่อน — รูปนี้ไม่ติดไปกับข้อความที่คัดลอก. **แก้บั๊กปัดเศษทศนิยม** ที่ทำให้งานลงตัวพอดีถูกคิดเกินไป 1 ท่อน (เช่น 4.08 ÷ 2.04 เคยได้ 3 ท่อน ตอนนี้ได้ 2 ท่อน) — บั๊กนี้จะเจอบ่อยขึ้นมากเมื่อพิมพ์หน้ากว้างเอง. bump `sw.js` → v22
- **31 ก.ค. 2026** — **เพิ่มเครื่องคำนวณราคาผ้าใบ** `fabric-price-calculator.html` สร้างจากตารางราคาขายผ้าใบ (Fabric Price, Update 15 Oct 2025): ผ้า **28 รายการ** (Solar Series 20 · SIOEN 7 · ECO-LINE 1) · **2 โหมดราคา** — ราคาเฉพาะผ้า (ต่อเมตร หรือ ต่อ ตร.ม. ตามชนิดผ้า) และ ราคาตัดเย็บ **Price A / B / C** ต่อ ตร.ม. · **คิดเศษอัตโนมัติตามหน้ากว้างผ้า** (งานเล็กกว่าหน้าผ้าคิดเต็มหน้าผ้า งานกว้างกว่าหน้าผ้าคิดเป็นท่อน) พร้อมแสดงเศษที่คิดเป็น ตร.ม. · เพิ่มด้ายเย็บ (DABOND `600` · Outdoor Pro 40 `1,350` · TENARA `6,000` · CUSBOR `4,000` บาท/หลอด) · Solar Trim `15` บาท/หลา · ค่าขนส่ง+ติดตั้งกรณีเปลี่ยนผ้าใช้โครงเดิม `300` บาท/ตร.ม. (ขั้นต่ำ `3,000`). เพิ่มหมวด **Fabric Only** ใน `index.html`. bump `sw.js` → v21
- **23 ก.ค. 2026** — **เพิ่มช่องติ๊ก 3 รายการใหม่** ในกลุ่มอุปกรณ์เสริม Somfy ของ **10 เครื่องคำนวณ**: รีโมท 6 ช่อง (`13,675`) · รีโมท 16 ช่อง (`12,432`) · สวิตซ์ Smoove 1 ช่อง RTS Origin (`2,362`) — ราคาอ่านจาก `prices.js` ทั้งหมด (คีย์ `remote6` / `remote16` / `smoove`) ป้ายราคาอัปเดตอัตโนมัติ. ไฟล์ที่เพิ่ม: `seatel-st2000`, `chill-d1`, `izip-screen`, `seatel-st1000e-vertical-awning`, `terra-ma3`, `spettmann-star` และไฟล์เฉพาะโครงสร้างทั้ง 4
- **23 ก.ค. 2026** — **`seatel-st2000.html` เพิ่มช่องติ๊กรีโมทครบชุด** (เดิมมีแต่เซ็นเซอร์ ไม่มีรีโมทเลย): รีโมท 1 · 2 · 5 · 6 · 16 ช่อง และสวิตซ์ Smoove อ่านราคาจาก `prices.js` ทั้งหมด พร้อมหมายเหตุเตือนว่ามอเตอร์บางรุ่นรวมรีโมท 1 ช่องมาแล้ว ให้ติ๊กเฉพาะรีโมทที่ต้องการเพิ่ม (กันคิดราคารีโมทซ้ำ)
- **23 ก.ค. 2026** — แก้ราคาสำรอง (`data-price`) ใน `seatel-st1000e-vertical-awning.html` ที่ยังค้างเลขเก่า (รีโมท 1/2/5 ช่อง = 3,017 / 3,862 / 6,035) ให้ตรงกับ catalog (3,108 / 3,978 / 6,216). bump `sw.js` → v19
- **23 ก.ค. 2026** — ย้าย **ราคาฝาครอบกันน้ำ (Cover Profile)** ไปไว้ในบล็อก `coverProfile` ของ `prices.js` = single source of truth: ฝาครอบอลูมิเนียม/สีเงิน MF `2,400` · ฝาครอบสีขาว WH `2,900` (**หน่วยเป็นบาทต่อความกว้าง 1 เมตร** ราคาจริง = ราคาต่อเมตร × ความกว้าง). ทั้ง **7 เครื่องคำนวณ** ที่มีฝาครอบดึงราคาจากที่นี่แล้ว (`seatel-st2000`, `seatel-st1000e-vertical-awning`, `chill-d1`, `ma4-calculator`, `spettmann-star`, `terra-ma3`, `terra-ma3-LED`) — ทั้งตัวเลขที่ใช้คำนวณและตัวเลขบนป้ายตัวเลือกอัปเดตอัตโนมัติ. เพิ่ม `<script src="prices.js">` ให้ `terra-ma3-LED.html` ซึ่งเดิมยังไม่ได้โหลด catalog. bump `sw.js` → v18
- **23 ก.ค. 2026** — แก้ **syntax error ใน `prices.js`** ที่ทำให้ไฟล์โหลดไม่ได้ทั้งไฟล์ (คีย์ `สวิตซ์ Smoove` มีเว้นวรรค ใช้เป็นชื่อคีย์ใน JavaScript ไม่ได้) → เปลี่ยนชื่อคีย์เป็น `smoove` และย้ายคำอธิบายภาษาไทยไปไว้ในคอมเมนต์. **ข้อควรระวัง:** ชื่อคีย์ต้องเป็นภาษาอังกฤษติดกัน ห้ามมีเว้นวรรค มิฉะนั้นเครื่องคำนวณทุกตัวที่โหลด `prices.js` จะคำนวณราคาอุปกรณ์เสริมไม่ได้ทั้งหมด
- **23 ก.ค. 2026** — **ปรับราคารีโมท Somfy:** รีโมท 1 ช่อง `3,017 → 3,108` · 2 ช่อง `3,862 → 3,978` · 5 ช่อง `6,035 → 6,216`. **เพิ่มรายการใหม่ใน `prices.js`:** รีโมท 6 ช่อง (`remote6` = 13,675) · รีโมท 16 ช่อง (`remote16` = 12,432) · สวิตซ์ Smoove 1 ช่อง RTS Origin (`smoove` = 2,362) — รายการใหม่ 3 ตัวนี้ยังไม่ได้ผูกกับช่องติ๊กในเครื่องคำนวณตัวไหน (เก็บไว้ใน catalog รอใช้งาน)
- **23 ก.ค. 2026** — **ปรับราคามอเตอร์ในบล็อก `motors`:** Somfy Altus 40/17 `26,500 → 26,800` · Altus 85/17 `36,500 → 36,800` · Maestria 15/17 `24,500 → 25,000` · Maestria 35/17 `30,500 → 31,000`. deploy ขึ้น Cloudflare แล้ว (ตัวอย่างโค้ดในหัวข้อ "กรณีพิเศษ" ด้านบนเป็นภาพประกอบ ตัวเลขจริงให้ยึดตาม `prices.js` เสมอ)
- **22 ก.ค. 2026** — ลบหัวข้อซ้ำซ้อน **"Customer copy text / ข้อความสำหรับส่งลูกค้า"** ที่อยู่ใต้หัวข้อ "Quotation Preview / ข้อความส่งลูกค้า" ออกจากทุกเครื่องคำนวณที่มี (15 ไฟล์ รวม zipguide-expert/ultra, izip-screen, terra-fs ทุกตัว ฯลฯ) — เป็นข้อความบอกซ้ำจึงเอาออก. ป้ายข้อความในเนื้อข้อความที่คัดลอกส่งลูกค้า (customer-copy-label) ยังคงไว้เหมือนเดิม. bump `sw.js` → v17 และ deploy ขึ้น Cloudflare แล้ว
- **22 ก.ค. 2026** — ขยายบล็อก `motors` ใน `prices.js` ให้ครอบคลุมมอเตอร์ Somfy ทุกรุ่นที่ใช้ในเครื่องคำนวณแบบเลือกมอเตอร์ได้ และเชื่อมไฟล์ `chill-d1`, `seatel-st2000`, `seatel-st1000e-vertical-awning`, `izip-screen` ให้ดึงราคามอเตอร์จาก `prices.js` (ผ่าน `data-price-key` ในแต่ละ `<option>`) = แก้ราคามอเตอร์ที่ `prices.js` ที่เดียว เปลี่ยนพร้อมกันทุกไฟล์. **ปรับราคา:** Somfy Altus 85/17 `32,800 → 36,500` · Altus 40/17 `25,800 → 26,500` (seatel-st2000) · Altus 30/17 `24,500 → 24,800` (seatel-st1000e). ไฟล์ที่ราคามอเตอร์รวมในตารางแล้ว (`ma4-calculator`, `terra-ma3`, `spettmann-star`) ไม่ถูกแก้ (แต่ตัวเลือก Somfy 85/17 ที่อ่านจาก catalog จะเป็น 36,500 ตามด้วย)
- **22 ก.ค. 2026** — ย้าย **ราคาตัวมอเตอร์** (Somfy Altus 50/12 · 85/17 · Strong Up 50/12 · 50/12+มือหมุน · 80/15) ไปไว้ในบล็อก `motors` ของ `prices.js` = single source of truth; ไฟล์เฉพาะโครงสร้างทั้ง 4 อ่านราคาจาก catalog แทนการฝังในโค้ด และเพิ่มตัวเลือก **Strong Up 50/12 + มือหมุน (11,900)** ป้ายราคาในช่องเลือกมอเตอร์อัปเดตอัตโนมัติจาก catalog
- **22 ก.ค. 2026** — เพิ่มการเลือก **มอเตอร์ + อุปกรณ์เสริม** ในไฟล์เฉพาะโครงสร้างทั้ง 4 ไฟล์ (`ma4-calculator-structure.html`, `terra-ma3-structure.html`, `seatel-st2000-structure.html`, `spettmann-star-structure.html`): มอเตอร์ Somfy Altus 50/12 · 85/17 และ Strong Up 50/12 · 80/15 พร้อมอุปกรณ์เสริม Somfy/Strong Up ที่อ่านราคาจาก `prices.js` — ไฟล์เหล่านี้เริ่มโหลด `prices.js` ด้วย

---

*ปรับปรุงล่าสุด: 1 ส.ค. 2026 · ใช้กับโปรเจกต์ Sangthong Awning Calculator · เว็บจริง: awning-calculator.pages.dev (Cloudflare Pages)*
